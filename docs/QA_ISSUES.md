# QA Issue Tracker — তোমার নিজের প্রসেসর বানাও

প্রতি অধ্যায় থেকে ১০টি করে candidate issue (deep per-chapter audit, 25 অধ্যায়)।

**মোট: 250**  ·  HIGH: 75  ·  MED: 103  ·  LOW: 71  ·  **Fixed: 29/250**

ব্যবহার: যেটা ঠিক হবে সেটার `[ ]` → `[x]` করো; কমিট-মেসেজে ref দাও। `[~]` = খারাপ-positive/বাতিল।

Legend: **[HIGH]** আসল বাগ/ভুল · **[MED]** গুরুত্বপূর্ণ · **[LOW]** nitpick/style।

> দ্রষ্টব্য: এগুলো এজেন্ট-যাচাইকৃত candidate; ঠিক করার আগে প্রতিটি current file-এর সাথে মিলিয়ে নেওয়া হবে (কিছু সাম্প্রতিক fix-এর সাথে overlap করতে পারে)।

---

## Chapter 1 — Digital Logic Introduction (10 issues: 2 HIGH, 5 MED, 3 LOW)

- [x] [HIGH] L138-147 — Transistor-as-switch diagram: drawn as Output→transistor→GND (pull-down), so "Gate ON → current flows" pulls Output to 0, contradicting the label "Output = 1". FIX: redraw with output pulled to VDD when ON, or relabel to match pull-down topology.
- [x] [HIGH] L125-130 — Voltage-level waveform malformed (top/bottom edges don't align into a valid square wave); also "5V ... তোমার FPGA তেও এমন" but Tang Nano 9K is 3.3V. FIX: redraw aligned square wave; change 5V→3.3V (or mark illustrative).
- [ ] [MED] L269 — "মাত্র ৭টা gate দিয়েই ... বানানো যায়" framed as the amazing fact, undercutting the later (correct) NAND-universality point at L439. FIX: reword; note NAND alone suffices.
- [ ] [MED] L182 — Reliability figures 100/60/30% decelerate (40 then 30 drop), contradicting "মান যত বাড়ে নির্ভরযোগ্যতা তত দ্রুত কমে". FIX: use 100/50/25%.
- [ ] [MED] L123-130 — single dashed line labeled both "Threshold ~2.5V" and "(Noise margin)"; noise margin is a band (V_IH–V_IL), not one line; contradicts prose. FIX: show two lines for the margin band.
- [ ] [MED] L686 — "দুইটা bit" inconsistent with "দুটো" used 41× elsewhere. FIX: "দুটো bit".
- [ ] [MED] L467-469 — NOT-from-NAND diagram ambiguous (doesn't clearly tie A to both NAND inputs). FIX: branch A to two input pins. (NOTE: I already rebuilt this diagram earlier — re-verify against current file.)
- [ ] [LOW] L253 — 32-bit table row breaks pattern: "4+ billion" vague + empty range column. FIX: 4,294,967,296 and 0–4294967295.
- [ ] [LOW] L362 — OR "1+1=1" justified as "output শুধু 0/1 হতে পারে" (clamping), misframing; it's logical disjunction by definition. FIX: reword.
- [ ] [LOW] L17 — trailing whitespace in checklist line inside code fence. FIX: strip.

## Chapter 2 — Number Systems & Boolean Algebra (10 issues: 1 HIGH, 4 MED, 5 LOW)

- [x] [HIGH] L319 — "Complement নিয়ম: X·Y + X·Y′ = X" mislabeled; that's the Combining/Adjacency theorem (the basis of K-map grouping), not Complement (A·A′=0). FIX: rename to Combining/Adjacency.
- [ ] [MED] L69 — "hardware-এর ... দুটো হাত" breaks the finger-base analogy (two hands ⇒ ten fingers). FIX: "দুটো অবস্থা (ON/OFF)".
- [ ] [MED] L215 — "১৮০ বছরেরও আগে" for Boole (1847/1854) overstates (~172-179 yrs). FIX: "প্রায় ১৭৫ বছর আগে".
- [ ] [MED] L252 — claims to show "Identity আর Null" but only gives a Null example (A+1=1); no Identity example. FIX: add A+0=A or narrow to Null.
- [ ] [MED] L426/L431 — promises to name each rule, but A+A·B=A (Absorption) is never named, and the 1+B=1 step (Null) is unnamed. FIX: label "Absorption" + "Null".
- [ ] [LOW] L282 — dangling English word "factor" at end of a Bengali annotation. FIX: drop or "(factoring)".
- [ ] [LOW] L127/L297/L411 — near-identical "try-it-yourself / answers at end" boilerplate repeated 3×. FIX: vary/trim.
- [ ] [LOW] L53/L123/L287 — trailing whitespace at line ends (not intentional hard-breaks). FIX: strip.
- [ ] [LOW] L123 — "101101₂ তে" detached locative; book elsewhere hyphenates (binary-তে). FIX: "101101₂-তে".
- [ ] [LOW] L33 — "বারবার অর্ধেক করার মতো" glosses over floor + remainder. FIX: clarify remainder is the discarded part.

## Chapter 3 — Combinational Circuits (10 issues: 2 HIGH, 5 MED, 3 LOW)

- [x] [HIGH] L880-903 — 1-bit ALU table defines 4 ops (AND/OR/ADD/SUB) but the 4:1 MUX in both mermaid & ASCII shows only 3 inputs (AND/OR/ADD), SUB missing. FIX: 4 inputs to the 4:1 MUX.
- [x] [HIGH] L475-493 — subtractor uses hardwired NOT gates (always invert B) but labels carry-in "Cin = Sub" as a mode signal; with permanent inverters Sub=0 gives A+(NOT B) = A−B−1, can't add. FIX: hardwire Cin=1 & call it a subtractor, OR use B XOR Sub gates.
- [ ] [MED] L26/L856/L1141 — prose lists "তুলনা" (compare) as a direct ALU op, but neither ALU op-table has compare (only via SUB + Z/N flags). FIX: remove or explain via subtraction+flags.
- [ ] [MED] L367 vs L381 — two adjacent 4-bit adder diagrams both label "(MSB)" on different signals (S3 in mermaid, Cout in ASCII). FIX: one convention.
- [ ] [MED] L49-53 / L175-179 — half-adder QuickWin diagram: B doesn't visibly reach the AND gate (Carry=A·B needs both). FIX: branch B into both gates.
- [ ] [MED] L919-934 vs L954-989 — INC (A+1)/DEC (A−1) listed with opcodes, but ALU arch shows only A,B inputs — no source for constant 1. FIX: show how 1 enters (force B=1 / use Cin).
- [ ] [MED] L1/L1171/L1216/L1226 — H1 says "Arithmetic Circuits" but it's Combinational (incl MUX/decoder/encoder); completion title inconsistent (3 names). FIX: rename H1 + standardize badge.
- [ ] [LOW] L550 — "5 outputs (S3-S0, overflow)" — 5th output is Cout, not overflow (L405 correctly says Cout). FIX: "Cout".
- [ ] [LOW] L529/L645/L894 — "তার-পর্যায়ে" awkward calque (×3). FIX: "wire-level-এ" / "তারের সংযোগে".
- [ ] [LOW] L254 — "মোট কয়টা 1-এর বিজোড়/জোড়" garbled. FIX: "Sum=1 যখন 1-এর সংখ্যা বিজোড়".

## Chapter 4 — Sequential Circuits (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [x] [HIGH] L46-47, L169-170, L189 — QUOTE: "S ──┐ / ├─[NOR]──┬── Q" (and truth-table row "| 1 | 0 | 1 | Set Q to 1 |") — PROBLEM: In both the Quick-Win diagram (L46-47) and the §4.2 diagram (L169-170), the Set input S feeds the NOR gate whose output is labelled Q. For a cross-coupled NOR latch that gate computes Q = NOR(S, Q'), so applying S=1, R=0 drives Q to 0 — the exact opposite of the truth table, which says S=1,R=0 → Q=1 (Set). As drawn, S resets Q instead of setting it; the wiring contradicts the truth table and the "S makes Q=1" semantics. (The D-latch in §4.3 wires D→S and assumes S=1→Q=1, confirming the intended semantics and that the diagram is the error.) — FIX: Swap the gate output labels so S feeds the gate that outputs Q' and R feeds the gate that outputs Q (canonical NOR SR latch: Q = NOR(R, Q'), Q' = NOR(S, Q)). Apply the same fix to both diagrams.
- [x] [HIGH] L927, L930 — QUOTE: "Send: 1101 (one bit per clock)" ... "Clock 2: 0100" — PROBLEM: The SIPO chain (L857) is Serial In → Q0 → Q1 → Q2 → Q3 with parallel output read as Q0Q1Q2Q3. If "1101" is sent first-bit-first (1,1,0,1), then after Clock 2 the register must be Q0=1 (new bit), Q1=1 (old Q0) → 1100, not 0100. The traced rows (1000, 0100, 1010, 1101) are only self-consistent if the bits are clocked in the order 1,0,1,1 (i.e. the reverse of the written value, or equivalently the final readout is Q3Q2Q1Q0). With "Send: 1101" the final Q0Q1Q2Q3 would be 1011, not 1101. The worked example does not reproduce. — FIX: Make bit order explicit and consistent: either state "send LSB-first: 1,0,1,1" and read the result as Q3Q2Q1Q0 = 1101, or keep first-bit-first and correct the rows to Clock1:1000, Clock2:1100, Clock3:0110, Clock4:1011.
- [x] [HIGH] L1079-1080, L1083 — QUOTE: "Circuit: Add NAND gate / Q3 · Q1 → Reset" (prose: "একটা NAND gate-ই যথেষ্ট। কেন `Q3 · Q1`?") — PROBLEM: The expression Q3 · Q1 is a logical AND (· = AND), which outputs 1 exactly when the count reaches 1010 (10) — correct for an active-HIGH synchronous reset. But the text calls it a "NAND gate." A NAND would output (Q3·Q1)' = 0 at count 10 and 1 elsewhere, i.e. the opposite polarity, which would not give an active-HIGH reset-at-10. The gate name (NAND) contradicts the written logic (AND) and the stated behaviour ("→ Reset to 0000" at 1010). — FIX: Change "Add NAND gate" to "Add AND gate" (Q3 · Q1 → active-HIGH Reset). If a NAND is genuinely intended, the reset must be active-LOW and the prose must say so.
- [ ] [MED] L1441, L1458, L1471 — QUOTE: "CLK ────────────────────┐ / └───────" (Setup; same falling-edge shape repeats for Hold L1458 and Tpd L1471) — PROBLEM: All three §4.12 timing diagrams draw the reference clock transition as a 1→0 (falling) edge. But the chapter's flip-flops are positive/rising-edge triggered (L411 "positive (rising) edge"; L434 arrows mark rising edges as the trigger points). Setup, hold, and clock-to-Q must be referenced to the rising (0→1) edge for these parts, so depicting a falling edge is inconsistent and can mislead the reader about which edge the constraints attach to. — FIX: Redraw the CLK in all three diagrams with a rising edge (0→1) at the reference point, e.g. CLK ──────┘‾‾‾‾, and align Tsu/Th/Tpd to that rising edge.
- [ ] [MED] L206-215 vs L169-178 — QUOTE: "S ──┐ / ├─[NAND]──┬── Q ... Note: NAND version - inputs are active LOW" — PROBLEM: The NAND SR-latch diagram is drawn byte-for-byte identically to the NOR diagram (same S→gate→Q mapping, same plain S/R labels, same feedback), with only a one-line note that inputs are active LOW. For an active-LOW (NAND) latch the inputs are conventionally S̅/R̅ and the asserting level is 0, so an identical picture with identical labelling gives the learner no visual cue of the difference and reinforces the wiring error in issue 1. — FIX: Distinguish the NAND version visually — label inputs S̅ and R̅ (or "S (active-LOW)"), and/or annotate the diagram to show that S=0 sets / R=0 resets, rather than reusing the NOR drawing verbatim.
- [ ] [MED] L1304-1310 vs L1326 — QUOTE: "Output ──┴── [Output Logic]" (circuit) vs "Output: Y = f(Q1,Q0,Input)" (build steps) — PROBLEM: The worked FSM is a Mealy machine (L1234: "এটা Mealy machine ... output state আর input — দুটোর উপর নির্ভর করবে") and the build step (L1326) correctly says the output is Y = f(Q1, Q0, Input). But in the circuit diagram the Output Logic block is fed only from the state line (Q1 Q0); there is no wire routing Input into Output Logic. As drawn it depicts a Moore output, contradicting the Mealy example and the equation. — FIX: Add an Input branch into the Output Logic block (Input must fan out to both Next-State Logic and Output Logic) so the diagram matches the Mealy Y = f(Q1,Q0,Input).
- [ ] [LOW] L185-190 — QUOTE: "| S | R | Q | Description | ... | 0 | 0 | Hold | ... | 1 | 1 | ??? | Invalid! (avoid) |" — PROBLEM: The column is headed "Q" (implying a Q value, 0/1) but its cells contain non-values: the word "Hold" and "???". A value column should not mix actual outputs (0,1) with behaviour words. Compare the JK table (L506) which correctly heads the column "Q(t+1)" so entries like Q(t)/Q'(t) fit. — FIX: Rename the header to "Q(next)" / "Q(t+1)" (and optionally write the hold row as "Q(t)" instead of "Hold"), so the column heading matches its contents.
- [ ] [LOW] L712-714 — QUOTE: "D3 ──┬──[MUX]──[D FF]── Q3 / Q3 ─┘" — PROBLEM: The enable MUX needs two separate data inputs (new data D3 and the fed-back Q3), one selected by EN. The ASCII ties the Q3 feedback into the same tee node as D3 *before* the MUX, so visually D3 and Q3 merge onto one wire entering the MUX rather than entering as the two distinct MUX inputs. The picture does not represent a 2:1 MUX (the prose at L723 describes it correctly). — FIX: Draw D3 into one MUX input and Q3 feedback into the other MUX input separately (D3 → MUX·0, Q3 → MUX·1, EN as select), not joined at a tee.
- [ ] [MED] L989, L992 vs L411 — QUOTE: "Each FF triggered by previous Q output" and "Q0 যখন 1 থেকে 0-তে নামে, সেই পতনটাই Q1-কে toggle করায়" — PROBLEM: For the ripple UP-count table (L996-1006: 0,1,2,3,...) to be correct when each FF is clocked by the previous stage's Q, the flip-flops must trigger on the previous Q's falling edge (1→0), i.e. be negative-edge triggered. The chapter's established default flip-flop is positive/rising-edge (L411, L434). If the ripple FFs were rising-edge clocked by the previous Q, the count would run DOWN (0,15,14,...), not up. The prose correctly relies on the falling edge but never states these FFs differ from the rising-edge default, leaving an unflagged contradiction. — FIX: State explicitly that the ripple up-counter uses negative-edge-triggered T flip-flops (each clocked by the previous Q's 1→0 transition), or clock each stage from the previous Q' on rising-edge FFs.
- [ ] [LOW] L1190 — QUOTE: "প্রতিটা বৃত্ত (বা গোল box) একটা state" — PROBLEM: "গোল box" awkwardly mixes the Bengali adjective গোল (round) with the English noun box, and is redundant right after বৃত্ত (circle) — "circle (or round box)" reads clumsily. — FIX: Use a clean term, e.g. "প্রতিটা বৃত্ত (বা node/গোল চিহ্ন) একটা state" or simply "প্রতিটা বৃত্ত একটা state, আর তীরগুলো..." (drop the parenthetical).

## Chapter 5 — Verilog Basics (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [x] [HIGH] L252 — QUOTE: "`verilog [7:0] means: bit 7 is MSB, bit 0 is LSB `" — PROBLEM: This bare English line sits inside a fenced `verilog block but is NOT a comment (no `//` prefix). As written it is illegal Verilog and would fail to compile if a learner copied the block — exactly the "code snippet that wouldn't compile" failure mode. The two preceding lines in the same block are also documentation, but they are proper `//`-comments or real statements; only this one is unprotected. — FIX: Prefix with a comment marker: `// [7:0] means: bit 7 is MSB, bit 0 is LSB`.
- [x] [HIGH] L23 / L518 / L528 — QUOTE: L23 "✅ 4-bit Adder - in 5 lines!" vs L518 "এখানে পুরোটা **এক লাইনে**:" vs L528 "// Just 2 lines for 4-bit adder!" — PROBLEM: Three contradictory line counts for the exact same `adder_4bit` (which contains a single `assign {cout, sum} = a + b + cin;`). The intro promises "5 lines", the prose says "one line" (এক লাইনে), and the in-code comment says "2 lines". A reader cannot reconcile these. — FIX: Pick one accurate description. The core logic is literally one `assign` line; use "এক লাইনে / one line" consistently and change the comment to `// Just 1 line for the whole 4-bit adder!` and reword the L23 bullet to match (e.g. "4-bit Adder - in 1 line!").
- [x] [HIGH] L24 — QUOTE: "✅ MUX/Decoder - with case statements" — PROBLEM: Factually wrong about the chapter's own content. The MUX modules (`mux_2to1` L507, `mux_4to1` L606) are built with the **ternary/conditional operator** (`? :`), and the decoder (`decoder_2to4` L633-636) is built with plain `assign` statements. The only `case` statement in the entire chapter is in `alu_4bit` (L580). No MUX or decoder here uses `case`. — FIX: Change to reflect reality, e.g. "✅ MUX/Decoder - with conditional & assign" (and, if `case` should be highlighted, attribute it to the ALU).
- [ ] [MED] L528 — QUOTE: "// Just 2 lines for 4-bit adder!" — PROBLEM: Even taken on its own (independent of issue 2), the comment is inaccurate: there is exactly **one** functional line (`assign {cout, sum} = a + b + cin;`). The "2 lines" likely miscounts the trailing decorative comment as code. An in-code claim that miscounts the code directly beneath it undermines trust. — FIX: `// Just 1 line for a full 4-bit adder!`
- [ ] [MED] L349 vs L114/L803-L804 — QUOTE: L349 "<= // Less than or equal" vs L804 "data_out <= data_in;" — PROBLEM: The token `<=` is introduced (L342-349) purely as the relational "less-than-or-equal" operator, but it is then used as the **non-blocking assignment** operator in the "good style" example (L803-804) and in the abstraction-level diagram (L114, `q <= d;`) with zero explanation that `<=` is overloaded. A beginner who just memorized `<=` = "less-or-equal" will badly misread the sequential examples. — FIX: Add a one-line caution where `<=` first appears in a sequential context (or in §5.4) noting that inside `always` blocks `<=` means non-blocking assignment, fully covered in Chapter 6.
- [ ] [MED] L1114-L1116 — QUOTE: "// Error 3: Size mismatch wire [7:0] result; assign result = 4'b1010; // Size mismatch warning" — PROBLEM: Assigning a narrower sized literal to a wider net is **legal, well-defined zero-extension** in Verilog, and Icarus Verilog (the chapter's tool) does **not** emit a warning for it. Calling it an "Error" (Error 3) and asserting a "warning" is fired overstates the situation and will confuse learners who try it and see clean output. (Contrast: the genuinely dangerous truncation case is RHS wider than LHS.) — FIX: Reclassify as a benign note, e.g. "নিরাপদ কিন্তু খেয়াল রাখো — ছোট মান বড় তারে গেলে বাকিটা 0 দিয়ে ভরে যায়; iverilog সাধারণত warning দেয় না।" Remove the "Error" framing or move it out of the Errors list.
- [ ] [MED] L944 / L887 — QUOTE: L944 "এখানে দুটো নতুন জিনিস লক্ষ করো। প্রথমত `$display` ..." (presented as new) while L887 already used `$display` is fine, BUT the deeper issue: L944 says the 4-bit-adder testbench shows "দুটো নতুন জিনিস" and counts `$display` as one — yet `$display` is genuinely first introduced only here, whereas `$monitor`/`$finish`/`initial`/`#delay` were all introduced in §5.10. PROBLEM: The "two new things" claim is loose — only `$display` and the edge-case test selection are arguably new, and the sentence lumps the well-known `$monitor` heading line in, making the count read as inconsistent with §5.11 where `$display` is again introduced "fresh". — FIX: Tighten to "একটা নতুন system task (`$display`) আর একটা নতুন কৌশল (edge-case পরীক্ষা)", and avoid re-introducing `$display` as brand-new again in §5.11 L976.
- [ ] [LOW] L601 — QUOTE: "input [3:0] d, // 4 data inputs (d[0] to d[3])" — PROBLEM: The comment lists the bits low-to-high as "d[0] to d[3]", contradicting the MSB-first `[3:0]` convention the chapter just taught (L252/L257: left = MSB). For a packed `[3:0]` vector the natural ordering is d[3]..d[0]. Minor, but it works against the convention drilled a few sections earlier. — FIX: "// 4 data inputs (d[3] down to d[0])" or simply "// 4 data inputs".
- [ ] [LOW] L227 — QUOTE: "integer i; // 32-bit signed integer" — PROBLEM: The width comment is only conditionally true. In Verilog `integer` is "at least 32 bits", and the value is implementation-defined width (commonly exactly 32). Stating a flat "32-bit" as fact is a slight oversimplification, and the chapter elsewhere is careful about sizes. — FIX: "// signed integer (at least 32-bit)" — or keep 32-bit but drop the over-precise framing.
- [ ] [LOW] L234 — QUOTE: "loop চালাতে বা গোনাগুনতি করতে দারুণ কাজের" — PROBLEM: "গোনাগুনতি" is a clumsy/non-standard colloquialism; the idiomatic word is "গোনাগুনতি"→"গণনা" or "গোনাগুনতি"→"হিসাব রাখা / গোনা". Combined with the immediately preceding "loop চালাতে" this reads awkwardly. — FIX: "loop চালাতে বা গণনা করতে দারুণ কাজের" (or "...গোনা-গুনতির কাজে").

## Chapter 6 — Always Blocks (10 issues: 4 HIGH, 4 MED, 2 LOW)

- [x] [HIGH] L198 — QUOTE: "// Result: a gets b, c gets OLD a (swap!)" — PROBLEM: This is factually wrong and pedagogically harmful in the chapter's single most important section. For `a <= b; c <= a;`, the result is a=b and c=old_a — that is a pass-through/registered copy, NOT a swap. A swap is exactly what Q3 (`x<=y; y<=x;`) at L1325-1338 demonstrates ("Swap! x and y exchange values"). Here neither variable receives the other's old value in a swapping manner (b is an external input, not part of any exchange). Calling this a "swap" directly contradicts the chapter's own correct swap example and will confuse the reader about what non-blocking does. — FIX: Change to `// Result: a gets b, c gets OLD a (not a pass-through!)` or `(c keeps old a, no chaining)`. Remove the word "swap".
- [x] [HIGH] L1110 — QUOTE: "S2 --> S1: in=1 / detected=1" combined with L1053 "localparam S2 = 2'b10; // Got 10" and L1083 "next_state = S2; // Got \"10\"" — PROBLEM: The state semantics are inconsistent/mislabeled relative to a correct "101" overlapping detector. With S1="got 1" and S2="got 10": from S2 (seen "10"), input `1` completes "101" — that is fine. BUT after detection the code sends S2 -> S1 (L1089), and the prose (L1114) justifies this as "the last 1 can be the first 1 of the next 101 (overlapping)". That reasoning is correct ONLY if S1 truly means "ends in 1". However the trace breaks for input "1011": after detecting at the second `1`, next is `0` from S1 -> S2 (correct, now "10"), but the design never models having just seen the final `1` as a fresh start from S0-equivalent — the labeling "Got 10" for S2 is reached from S1 on `0`, which is right, yet the diagram omits the self-consistent meaning. The concrete defect: the diagram/comment claim overlapping support, but the comment at L1081 `S1: if(in) next_state=S1; // Stay (multiple 1s)` mislabels intent — it should note "ends in 1, restart-friendly", not "multiple 1s". The reader cannot reconcile "Got 1" / "multiple 1s" with the overlap claim. — FIX: Relabel states by suffix-meaning (S0="", S1="...1", S2="...10") and fix the L1081 comment to "stay; still ends in a single 1". Verify the diagram edge labels match these suffix meanings.
- [x] [HIGH] L946-948 vs L955-961 vs L1016 — QUOTE: "RED → 30 seconds / GREEN → 25 seconds / YELLOW → 5 seconds" and state cycle "RED → GREEN → YELLOW → RED" — PROBLEM: The "States" box presents concrete durations (30/25/5 s) as if the FSM enforces them, but the actual `traffic_light` module has NO timer, NO counter, and NO duration logic — it only reacts to an external `timer_done` pulse and holds otherwise. Nothing in the code makes RED last 30 s vs GREEN 25 s; all three states behave identically (wait for `timer_done`). Presenting specific second-counts next to code that cannot produce them is misleading; a learner will expect different per-state timing and find none. — FIX: Either remove the numeric durations (replace with "each state waits until timer_done") or add the note that per-state durations require an external timer feeding `timer_done`, which this module does not implement.
- [x] [HIGH] L331 — QUOTE: "reset যেহেতু সবার আগে চেক হয়, সে সবসময় জয়ী — তাই একে বলে reset-এর \"priority\"।" — PROBLEM: This explains asynchronous-reset priority by the wrong mechanism. In the async block `always @(posedge clk or posedge reset)`, reset wins because it is in the sensitivity list (the block fires immediately on reset's edge, independent of clock) — NOT merely because `if (reset)` is written first. The "checked first" reasoning is actually the explanation for SYNCHRONOUS reset priority (L344-350), where ordering inside the clock-only block matters. Conflating the two undermines the very async-vs-sync distinction the section (L333-360) is teaching. — FIX: For the async case, attribute priority to reset being in the sensitivity list (asynchronous, fires regardless of clock); reserve the "checked first / if-ordering" explanation for the synchronous-reset subsection.
- [ ] [MED] L242 — QUOTE: "ফলে q0-তে আসা নতুন `d` এক ধাপ এক ধাপ করে এগোয় না, বরং এক cycle-এই সব stage গড়িয়ে যায়" — PROBLEM: For the WRONG blocking shift register (L224-228: `q2=q1; q1=q0; q0=d;` in top-to-bottom order), `d` does NOT propagate through all stages in one cycle. Because q2 is assigned BEFORE q1 and q0 are updated, q2 gets old q1 and q1 gets old q0 — so the blocking bug here actually still behaves like a (correct) shift for this particular ordering; the classic "d flows through everything in one clock" disaster happens with the REVERSE ordering (`q0=d; q1=q0; q2=q1;`). The text's own code comment at L229 ("d flows through all in ONE clock!") and this prose are both wrong for the code as written. — FIX: Either reverse the assignment order in the WRONG example to `q0=d; q1=q0; q2=q1;` (which truly collapses to one cycle), or correct the prose/comment to state that the bug's effect depends on statement order and this ordering happens to still shift.
- [ ] [MED] L61 vs L39 — QUOTE: "**এই 8 lines = একটা D Flip-Flop chip!**" and L39 "নিচের ৮ লাইন লিখলেই" — PROBLEM: The `d_ff` module (L47-56) is not 8 lines. Counting the code block it is 10 physical lines (module header through endmodule), or 9 excluding the comment, or 6 lines of actual statements. "8 lines" matches none of these. Minor but it is a concrete, checkable factual claim repeated twice. — FIX: Change both to a count that matches the shown code (e.g., "৯ লাইন" excluding the comment, or describe it as "এই ছোট module").
- [ ] [MED] L383 — QUOTE: "এই দুটো পরিস্থিতি গুলিয়ে ফেলো না — sensitivity list-ই এখানে আসল পার্থক্য।" combined with L65 — PROBLEM (calque / unclear): "গুলিয়ে ফেলো না" is fine, but the surrounding claim "sensitivity list-ই এখানে আসল পার্থক্য" is imprecise: the difference between a benign missing-else (sequential hold) and a latch-creating missing-else is whether the block is edge-triggered vs level/`@(*)` — i.e., the block TYPE, which the sensitivity list only indirectly signals. A reader could wrongly infer that adding signals to a `@(*)` list changes latch behavior. — FIX: Reword to "always-block-টা clock-edge-triggered না combinational — সেটাই আসল পার্থক্য" to name the actual determinant rather than the sensitivity list as such.
- [ ] [MED] L65 — QUOTE: "`reg` মানে \"এই signal-এর value আমি always block-এ procedurally set করব\"।" — PROBLEM: Incomplete/slightly inaccurate. A `reg` is required for any variable assigned in ANY procedural block — `always` AND `initial` (and tasks/functions). Restricting the explanation to "always block" is too narrow; testbenches in Chapter 7 will assign `reg`s in `initial` blocks, contradicting this definition. — FIX: "procedural block (always বা initial)-এ" instead of "always block এ".
- [ ] [LOW] L623 — QUOTE: "বাস্তবে **`casez` ব্যবহার করাই নিরাপদ** — কারণ `casex` ভুলবশত input-এর `x` (unknown) মানকেও don't-care ধরে নিয়ে সিমুলেশনে আসল bug ঢেকে দিতে পারে।" — PROBLEM: Partially incomplete: `casez` also treats `z` in the CASE EXPRESSION (the scrutinee), not only in the labels, as don't-care, which can likewise mask bugs if a real `z` reaches the input. The blanket "casez is safe" is an oversimplification; the genuinely safest modern guidance is to avoid both for synthesis or use explicit comparisons. Minor since casez is indeed the lesser evil. — FIX: Add a half-sentence that casez still treats `z` on the input side as don't-care, so it is "safer, not fully safe".
- [ ] [LOW] L825 — QUOTE: "নাম তিনটে অক্ষরে বোঝা যায়: S = Serial, P = Parallel, I = In, O = Out।" — PROBLEM: Internal contradiction: it says the names are understood by "তিনটে অক্ষর" (three letters), but the acronyms SISO/SIPO/PISO are FOUR letters and the legend itself lists FOUR mappings (S, P, I, O). "তিনটে" is simply the wrong number. — FIX: Change "তিনটে অক্ষরে" to "চারটে অক্ষরে" (four letters).

## Chapter 7 — Testbenches (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [x] [HIGH] L266-279 — QUOTE: " #3 clk = 1; // High for 3ns" / " #7 clk = 0; // Low for 7ns" ... "// 30% duty cycle" ... "এখানে 3ns high আর 7ns low, মোট period 10ns, তাই high থাকে 3/10 = 30% সময়।" — PROBLEM: The delay precedes the assignment, so `#3` is the time clk stays LOW (3ns) and `#7` is the time clk stays HIGH (7ns). Verified by simulation: steady-state clk is HIGH 7ns, LOW 3ns → duty cycle = 7/10 = 70%, NOT 30%. Both inline comments are reversed and the prose math (and "30%") is wrong. — FIX: Either swap the comments to `// Low for 3ns` / `// High for 7ns` and state "high থাকে 7/10 = 70%", or to actually get 30% reorder to `#3 clk=1; #7 clk=0;` becoming `#7 clk=0` first / set clk=1 for 3ns then 0 for 7ns (i.e. `clk=1; #3 clk=0; #7 clk=1;` pattern). Make code, comments, and prose agree.
- [x] [HIGH] L961, L963 — QUOTE: "`$random % 16` মানে random সংখ্যাকে 16 দিয়ে ভাগ করে ভাগশেষ — যা সবসময় 0 থেকে 15-এর মধ্যে থাকবে ... আর `5 + ($random % 6)` মানে: 0-5-এর একটা random-এর সাথে 5 যোগ — ফল 5 থেকে 10-এর মধ্যে।" then L963: "`$random` ঋণাত্মক সংখ্যাও দিতে পারে (এটা signed)। তাই `% 16` কখনো কখনো ঋণাত্মক ভাগশেষ দিতে পারে।" — PROBLEM: Internally self-contradicting and factually wrong. `$random` is signed, so `$random % 16` ∈ [-15,15] (verified: produced -15, -7, -13), and `5 + ($random % 6)` ∈ [0,10] (verified: produced 4, 2, 0), NOT "always 0-15" and NOT "5 to 10". The absolute claims on L961 are contradicted by the book's own warning on L963. — FIX: Use `{$random} % 16` (concatenation forces unsigned) or `$unsigned($random) % 16` for a true 0-15 range, and `5 + ($unsigned($random) % 6)` for 5-10; or correct L961 to state the real signed ranges.
- [x] [HIGH] L833, L862-878, L895 — QUOTE: test_vectors.txt begins "// a b expected_sum" and reader is "while (!$feof(file)) begin / status = $fscanf(file, \"%d %d %d\\n\", a, b, expected); / if (status == 3) begin ..." with prose L895 "এতে file-এর শেষের ফাঁকা লাইন বা comment ভুলবশত test হয়ে যায় না।" — PROBLEM: `$fscanf` with `%d` cannot match the leading `//` comment line; it returns 0 and does NOT advance the file pointer, so `$feof` never becomes true and the loop spins forever (verified: caught at 100+ iterations with status=0, a/b/expected=x; removing the comment line makes it run correctly). The `if(status==3)` guard prevents bad *checks* but does NOT prevent the infinite loop — directly contradicting L895. — FIX: Remove the `//` header from test_vectors.txt (the format Verilog `$fscanf` cannot parse comments), or read line-by-line with `$fgets` and skip `//` lines, or break the loop when `status != 3`/`status == -1`.
- [ ] [MED] L427-428 — QUOTE: "$finish(0); // Exit with status 0" / "$finish(1); // Exit with status 1" — PROBLEM: The integer argument to `$finish` is a *diagnostic verbosity level* (0 = print nothing, 1 = print simulation time and location, 2 = also print CPU/memory stats), NOT a process exit/return status code. Describing it as "exit with status N" is wrong and misleads readers into thinking it sets a shell exit code. — FIX: "$finish(0); // exit, print no message" / "$finish(1); // exit, print time & location (default)" / add "$finish(2); // also print run statistics".
- [ ] [MED] L800-805 — QUOTE: "$dumpvars(1, testbench.dut.signal1); / $dumpvars(1, testbench.dut.signal2); ... // Start dumping later / #100; / $dumpvars(0, testbench);" — PROBLEM: `$dumpvars` may be called only once per simulation; subsequent calls are ignored (verified with Icarus: "VCD warning: $dumpvars ignored, previously called at simtime 0" — only the first call took effect, the later `$dumpvars(0, testbench)` was dropped). So the "Start dumping later" idiom shown here does not work, and dumping two signals via two separate `$dumpvars` calls also fails (only signal1 is dumped). — FIX: Combine targets into one `$dumpvars` call; for delayed dumping use `initial $dumpoff;` then `#100 $dumpon;` (the `$dumpoff`/`$dumpon` already shown), not a second `$dumpvars`.
- [ ] [MED] L1087-1092 — QUOTE: " always @(posedge clk) begin / if ((red + yellow + green) != 1) begin / $display(\"ERROR: Multiple lights on!\"); / $finish; end" — PROBLEM: Two issues. (a) The invariant is not gated on `!reset`; it fires on the first posedge (t=5) while reset is still asserted (reset is deasserted only at the 3rd posedge, t=25). If the FSM holds all lights off during reset, `(0) != 1` is true and the assertion aborts the whole simulation with a false error before any real testing. (b) The message says "Multiple lights on!" but the condition `!= 1` is also true when ZERO lights are on, so the message is wrong for the all-off case. — FIX: Guard with `if (!reset && (red+yellow+green) != 1)` and reword the message to "Exactly one light must be on (got %0d)".
- [ ] [MED] L1136, L1163, L1172 — QUOTE: " reg [80*8:1] op_name;" used as "$display(\"PASS: %s %0d %0d = %0d ✓\", op_name, in_a, in_b, result);" — PROBLEM: `op_name` is 640 bits wide but holds 3-char strings ("ADD"), so with `%s` the unused high bytes print as a long run of blanks (verified: output is "[<~77 spaces>ADD]"). Every PASS/FAIL line is prefixed by ~77 spaces, garbling the very "readable" output the chapter promises (L1106: "সংখ্যার চেয়ে যা পড়তে অনেক সহজ"). — FIX: Size the reg to the longest name, e.g. `reg [3*8:1] op_name;` (or `[8*8:1]`), so `%s` prints just the name without huge leading padding.
- [ ] [LOW] L167, L397, L641, L1076 — QUOTE: "$monitor(\"Time=%0t a=%b b=%b sum=%d\", $time, a, b, sum);" (and similar `Time=%0t`) — PROBLEM: With `` `timescale 1ns/1ps `` and no `$timeformat`, `%0t` prints time in *precision* units, so 5ns shows as "5000", 10ns as "10000" (verified), not the clean "5"/"10" a reader expects from "Time=". The chapter never introduces `$timeformat`, so all timestamped output looks confusingly inflated. — FIX: Add an `initial $timeformat(-9, 0, " ns", 6);` once (or mention it), so `%t` prints readable nanosecond values.
- [ ] [LOW] L696 — QUOTE: "এই counter testbench-এ `repeat` এর সৌন্দর্যটা দেখো। `repeat(2) @(posedge clk)` দিয়ে reset-কে দুই cycle ধরে রাখা হলো।" — PROBLEM: With `clk=0` at t=0 toggling every 5ns, posedges fall at t=5 and t=15, so `repeat(2) @(posedge clk)` holds reset from t=0 to t=15 = 1.5 clock periods (15ns), not two full periods. Calling it "দুই cycle" of hold time is imprecise (it is 2 *edges* but 1.5 *periods*). — FIX: Say "দুটো rising-edge পর্যন্ত (≈১.৫ period)" or start the clock at `clk=1` so two posedges equal two full periods.
- [ ] [LOW] L800, L817 — QUOTE: "লক্ষ্য করো এখানে depth `1` দিয়ে `$dumpvars(1, testbench.dut.signal1)` — মানে শুধু ওই নির্দিষ্ট একটা signal, পুরো গাছ না।" — PROBLEM: The single-signal behavior comes from naming a leaf signal as the target, not from the depth value `1`. For an individual named signal the depth argument is irrelevant; the text wrongly attributes "only one signal" to `depth = 1`, which would mislead a reader into thinking `$dumpvars(1, some_module)` dumps only one signal (it actually dumps that module's level). — FIX: Clarify that depth controls how many hierarchy *levels below a module* are dumped, and that here only one signal is dumped because a single signal (not a module) was named.

## Chapter 8 — Advanced Verilog (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [x] [HIGH] L403/L407 — QUOTE: "localparam MAX_VALUE = (1 << WIDTH) - 1;" ... "wire [MAX_VALUE:0] wide_signal;" — PROBLEM: `MAX_VALUE` is the numeric *value* of a WIDTH-bit number (for WIDTH=8 it equals 255, as the text itself confirms at L413: "WIDTH=8 হলে এটা ২⁸−১ = ২৫৫"). Using that value as a vector bound `[MAX_VALUE:0]` therefore declares `wire [255:0] wide_signal` — a nonsensical 256-bit signal — for an 8-bit module. The example conflates "maximum representable value" with "bit index/width," which is exactly the conceptual error the localparam section is supposed to teach against. — FIX: Either change the declaration to use a width constant (e.g. `localparam WIDTH_PLUS = WIDTH + 1; wire [WIDTH_PLUS-1:0] wide_signal;`) or drop the `wide_signal` line and keep `MAX_VALUE` only as a comparison/saturation constant (e.g. `if (count == MAX_VALUE)`), so the reader does not learn to index a vector with a max-value constant.
- [x] [HIGH] L137/L153/L158 — QUOTE: "function integer count_leading_zeros;" ... "wire [5:0] clz;" ... "output ৬ bit চওড়া (`[5:0]`) কারণ ৩২টা শূন্য পর্যন্ত গোনার জন্য সর্বোচ্চ মান ৩২" — PROBLEM: The prose claims the function's output is "৬ bit চওড়া (`[5:0]`)," but the function is declared with return type `integer`, which is a 32-bit signed value, not a 6-bit `[5:0]` value. Only the receiving wire `clz` is `[5:0]`; assigning the 32-bit integer result to it silently truncates. The explanation misattributes the wire's width to the function and never mentions the width mismatch/truncation. — FIX: Declare the function to match its real output, e.g. `function [5:0] count_leading_zeros;` (or `function [$clog2(33)-1:0] ...`), and reword the prose to say "the function returns a 6-bit result" so the declaration and explanation agree.
- [x] [HIGH] L607-L624 vs L559/L657 — QUOTE: "case (INPUTS)\n 2: begin\n ... \n 4: begin\n ... \n default: begin" — PROBLEM: The chapter explicitly and twice establishes the rule "generate block-এ সবসময় নাম দাও — এটা পেশাদার অভ্যাস" (L559) and demonstrates labels everywhere else (`adder_stage`, `block_ram`, `distributed_ram`, `output_stage`, `input_mux`, `gray_gen`). But the `generate case` example's branches are all unlabeled `begin` blocks. This directly contradicts the chapter's own stated best practice in the one place where the named-block rule matters for `case`. — FIX: Add labels to every branch, e.g. `2: begin : mux2`, `4: begin : mux4`, `8: begin : mux8`, `default: begin : mux_generic`, consistent with the generate-for and generate-if examples.
- [ ] [MED] L641-L642 — QUOTE: "input [INPUTS-1:0][WIDTH-1:0] data_in,\n output [OUTPUTS-1:0][WIDTH-1:0] data_out" — PROBLEM: This is SystemVerilog multidimensional *packed array* port syntax. The entire book targets plain Verilog (`.v` files, Icarus Verilog per CLAUDE.md and L69), and every other module in this chapter uses Verilog-2001 single-range packed vectors (e.g. `[INPUTS*WIDTH-1:0] data_in` in the immediately preceding `mux_tree`). Standard Verilog-2001 does not support `[a-1:0][b-1:0]` packed port dimensions; this will not compile under the toolchain the book uses, and it is inconsistent with the book's own flattened-vector convention. — FIX: Flatten to the book's existing style, e.g. `input [INPUTS*WIDTH-1:0] data_in, output [OUTPUTS*WIDTH-1:0] data_out`, or explicitly note this requires SystemVerilog (and adjust the toolchain statement). Stay consistent with the `mux_tree` example two listings above.
- [ ] [MED] L267-L270 (module `uart_tx`, L240) — QUOTE: "always @(posedge clk) begin\n if (send)\n send_byte(data);\n end" — PROBLEM: `send_byte` (via `send_bit`) contains event controls `repeat(BAUD_TICKS) @(posedge clk);`. A task containing `@(posedge clk)` is not synthesizable, and embedding such a blocking, multi-cycle task inside an `always @(posedge clk)` block is illegal for synthesis (and would re-launch every cycle `send` is high). This is presented as a hardware `module uart_tx` — unlike the correctly-labeled *testbench* tasks `bus_write` (L283) and `init_memory` (L310). The 🚩 note at L276 only flags the missing `BAUD_TICKS` parameter and explicitly calls the rest "শেখানোর জন্য সরলীকৃত," implying the structure is otherwise valid hardware, which it is not. — FIX: Either relabel this as testbench/behavioral-model code (not a synthesizable module), or add a note that timed tasks like this are simulation-only and the real transmitter must be an FSM (as hinted at L274), so readers do not try to synthesize it.
- [ ] [MED] L48/L59 — QUOTE: "function automatic parity_calc;" ... "assign parity = parity_calc(data);" — PROBLEM: `parity_calc` is declared `automatic` but is non-recursive and pure combinational — the keyword is unnecessary here and is introduced before `automatic` is ever explained (its meaning is only taught later at L176-L196). The L65 walkthrough explains every other token ("`function automatic parity_calc;` দিয়ে একটা function ঘোষণা করলাম") but never says what `automatic` does, leaving a beginner with an unexplained keyword in the very first example. — FIX: Either drop `automatic` from the Quick Win (use `function parity_calc;`) since it is not needed, or add one clause noting "`automatic`-এর মানে পরে ৮.১-এ বুঝবে; এখানে এটা ছাড়াও চলত."
- [ ] [MED] L99/L121/L163/L137 — QUOTE: (L99) "function [return_width-1:0] function_name;" vs (L137) "function integer count_leading_zeros;" — PROBLEM: The "Basic Function Syntax" skeleton (L96-L109) and surrounding prose teach exactly one return-type form: a bit-range `[return_width-1:0]` ("প্রথম লাইনে `[return_width-1:0]` বলে দিচ্ছে ফলটা কত bit চওড়া — এটাই function-এর 'type'"). The chapter then silently uses two other return-type forms — `function integer ...` (L137, L182) and `function automatic integer ...` — without ever introducing that a function may return `integer` (or be untyped/1-bit as in `parity_calc`). The stated rule "কোথাও `[return_width-1:0]`" is presented as the form, so the `integer` returns look like they violate the just-taught syntax. — FIX: In the syntax section, mention the alternative return types (`integer`, default 1-bit when omitted) so the later `function integer ...` examples are consistent with what was taught.
- [ ] [LOW] L48-L54 — QUOTE: "function automatic parity_calc;\n input [7:0] data_in;\n integer i;" — PROBLEM: The function parameter is named `data_in`, but the surrounding module port is `data` and the call is `parity_calc(data)`. Meanwhile the very next worked function `max` reuses the outer port names (`x, y`) and CLZ uses `data`. The naming is inconsistent across the section's examples (sometimes `_in` suffix, sometimes not) with no rationale, which is confusing for a first example whose prose stresses the name. — FIX: Standardize the input-naming convention across the function examples (e.g. consistently use a `_in` suffix, or consistently not), so learners see one pattern.
- [ ] [LOW] L251/L276 — QUOTE: "repeat(BAUD_TICKS) @(posedge clk);" with note "এই module-এ `BAUD_TICKS` কোথাও parameter বা `localparam` হিসেবে ঘোষণা করা হয়নি ... বাস্তবে compile করার আগে module-এর header-এ `parameter BAUD_TICKS = ...;` যোগ করে নিতে হবে" — PROBLEM: `BAUD_TICKS` is used in upper-case identifier style (the book's convention for parameters/localparams), and the fix-up note frames it purely as a "missing declaration." But the deeper point — that `BAUD_TICKS` here represents *clock ticks per bit*, derived from clock frequency / baud rate — is never stated, so a reader who "just adds `parameter BAUD_TICKS = ...`" has no guidance on what value is meaningful. Combined with issue #5, the note understates what is actually wrong with the example. — FIX: Add a brief clause defining `BAUD_TICKS` as "clk frequency ÷ baud rate" so the placeholder is meaningful, not just syntactically present.
- [ ] [LOW] L832 — QUOTE: "`DEPTH` হওয়া উচিত `2**ADDR_WIDTH` (এখানে ১০-bit address মানে ঠিক ১০২৪টা ঘর, যা মিলে যায়)" and L811 "parameter DEPTH = 1024" with L810 "parameter ADDR_WIDTH = 10" — PROBLEM: The single_port_ram exposes `ADDR_WIDTH` and `DEPTH` as two independent parameters that must be kept consistent by hand, and the note correctly warns about this — yet the chapter elsewhere (FIFO L368, register_file L443, rom L884) teaches the robust pattern `$clog2(...)`/derive-one-from-the-other and even repeats the warning. Presenting the *un-derived* anti-pattern in a code listing (rather than just in prose), right after teaching the correct `$clog2` idiom, risks readers copying the fragile version. The "শেখার জন্য এখানে তিনটাই খোলা রাখা হয়েছে" justification is weaker here because nothing in the RAM body needs both independently. — FIX: Change the listing to `parameter ADDR_WIDTH = 10, localparam DEPTH = 2**ADDR_WIDTH;` (or `parameter DEPTH=1024, localparam ADDR_WIDTH=$clog2(DEPTH)`), matching the rom/FIFO examples, and keep the note as reinforcement.

## Chapter 9 — FPGA Architecture (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [x] [HIGH] L302/L316-L320 — QUOTE: "// Single 4-input LUT can implement:" ... "// Example 5: 4:1 MUX\nassign out = sel[1] ? (sel[0] ? d : c) :\n (sel[0] ? b : a);" ... "All in ONE LUT! Magic! ✨" — PROBLEM: Examples 1-4 use 4 inputs (a,b,c,d) and correctly fit a 4-input LUT, but Example 5 (a 4:1 MUX) needs 6 distinct inputs (a, b, c, d, sel[0], sel[1]). A 4-input LUT has only 2⁴=16 cells and cannot implement a 6-input function. The claim that this MUX fits in a "Single 4-input LUT" / "ONE LUT" is technically wrong — a 4:1 MUX requires a 6-input LUT. — FIX: Either change Example 5 to a genuine 4-input function (e.g. a 2:1 MUX `sel ? b : a` uses 3 inputs), or change the header to "Single 6-input LUT can implement:" and note that the MUX is the case that needs the larger LUT.
- [x] [HIGH] L83/L116 — QUOTE: "1984: First FPGA by Xilinx (XC2064)" and "১৯৮৪ সালের প্রথম FPGA-তে ছিল মাত্র **৬৪টা logic block**" — PROBLEM: 1984 is the year Xilinx was *founded*; the XC2064, the first FPGA product, was introduced/shipped in **1985**. The chapter itself separates these correctly at L908 ("Founded: 1984 (invented FPGA!)"), so dating the XC2064 product to 1984 is a factual error and internally inconsistent. — FIX: Change the product/timeline date to 1985 (e.g. "1985: First FPGA by Xilinx (XC2064)"), keeping "Founded 1984" for the company.
- [x] [HIGH] L1025/L1036 — QUOTE: "| **On-board clock** | ২৭ MHz | ৯.১২ — timing-এর reference frequency |" and "উপরের spec — **৮,৬৪০ LUT, ৬,৪৮০ FF, ৪৬৮ Kb BRAM, ২৭ MHz** — হলো Tang Nano 9K-তে বসানো GW1NR-9 চিপের আসল মান" — PROBLEM: 27 MHz is the frequency of the crystal oscillator placed on the Tang Nano 9K *board*, not an intrinsic spec of the GW1NR-9 *chip*. The chip has no fixed 27 MHz; it accepts any clock and has internal PLLs to retime it. The table is introduced (L1018-1020) as "the resources inside the chip," yet lists a board-level oscillator, and L1036 explicitly calls 27 MHz "GW1NR-9 চিপের আসল মান." (L1032 correctly says "board-এ ... একটা 27 MHz crystal বসানো," contradicting L1036.) — FIX: Label 27 MHz as a board feature, e.g. "Tang Nano 9K board-এর on-board oscillator (চিপের নয়)," and drop it from the list of chip resources or move it under a separate "board" heading.
- [ ] [MED] L537/L543/L548 — QUOTE: "একই 18Kb (≈18,000 bit) BRAM-কে তুমি ... নানা আকারে কনফিগার করতে পারো" and table rows "16K × 1 bit", "8K × 2 bits", "4K × 4 bits", then "লক্ষ্য করো — প্রতিটা সারিতে মোট bit সংখ্যা প্রায় একই (width × depth ≈ 18Kb)" — PROBLEM: The arithmetic is inconsistent. 16K×1 = 8K×2 = 4K×4 = 16,384 bits (≈16Kb), while 2K×9 = 1K×18 = 512×36 = 18,432 bits (≈18Kb). The non-parity configs total 16Kb of data, not ~18Kb; the extra capacity in the ×9/×18/×36 rows is parity. So the claim that "every row ≈ 18Kb" is false for the first three rows (off by ~12%). — FIX: State it accurately: an 18Kb block holds 16Kb (16,384) data bits plus 2Kb parity; show the ×1/×2/×4 rows as 16K/8K/4K (16Kb) and the ×9/×18/×36 (parity) rows as ~18Kb, instead of claiming all rows are equal.
- [ ] [MED] L116 — QUOTE: "১৯৮৪ সালের প্রথম FPGA-তে ছিল মাত্র **৬৪টা logic block** — আজকের high-end FPGA-তে থাকে **কয়েক মিলিয়ন** logic cell। অর্থাৎ চার দশকে capacity বেড়েছে প্রায় দশ লক্ষ গুণ!" — PROBLEM: The math contradicts its own figures. 64 → "a few million" (say 2-9 million) is roughly 30,000×-140,000× (about a lakh), not "দশ লক্ষ গুণ" (≈1,000,000×). To reach 1,000,000× from 64 you would need 64 million logic cells, which no FPGA has. The factor is overstated by about an order of magnitude. — FIX: Change to a defensible figure such as "প্রায় এক লক্ষ গুণ" (~100,000×) or "লক্ষাধিক গুণ," consistent with "few million / 64."
- [ ] [MED] L291/L295 vs L1022 — QUOTE: "| **4-input LUT** | 2⁴ = 16 bits | পুরোনো/ছোট FPGA |" and "industry দেখেছে যে **6-input LUT** এই দড়ি-টানাটানিতে সবচেয়ে ভালো সমঝোতা — তাই আজকের প্রায় সব আধুনিক FPGA (Xilinx, Intel সহ) 6-input LUT ব্যবহার করে" — PROBLEM: §9.3 frames 4-input LUTs as "old/small" and says nearly all modern FPGAs use 6-input LUTs, but the book's own target part, the Gowin GW1NR-9 on the Tang Nano 9K, uses **4-input LUTs (LUT4)** — confirmed by its datasheet (8,640 LUT4). The §9.11.5 "our chip" table (L1022) just says "LUT" and never reconciles this, leaving a reader to conclude the platform they will use is "old." — FIX: Add a clarifying note that small/low-cost FPGAs (including Gowin/Tang Nano 9K) still use 4-input LUTs, so "4-input" means "small/low-cost," not obsolete; and avoid implying *all* modern FPGAs are 6-input.
- [ ] [MED] L59 — QUOTE: "| **Power** | N/A | Medium | Low |" (Software (CPU) column) — PROBLEM: Listing a CPU's power as "N/A" is misleading. A general-purpose CPU consumes very real and typically the highest power per unit of work for a given task — that is precisely why FPGAs/ASICs are used as accelerators. The chapter's own narrative (L65-66, ASIC "কম-power," FPGA "Medium") implies CPU should be the high end, so "N/A" breaks the comparison. — FIX: Replace "N/A" with "High" (or "Higher") in the Software (CPU) / Power cell.
- [ ] [LOW] L979 vs L990 — QUOTE: "✅ GW1N-9 (Medium, Tang Nano 9K) ← We'll use!" then later "Tang Nano 9K-তে আসলে এই family-রই **GW1NR-9** variant-টি বসানো আছে" — PROBLEM: The Gowin family list attributes the Tang Nano 9K to the part "GW1N-9," but the actual chip on the board is the **GW1NR-9** (integrated PSRAM). The discrepancy is only corrected two notes later (L990) and in §9.11.5; the family bullet itself names the wrong part number for that board. — FIX: Make L979 read "GW1NR-9 (Medium, Tang Nano 9K)" (or "GW1N(R)-9") so the part number is correct at first mention.
- [ ] [LOW] L714-L715 — QUOTE: "| **SSTL** | 1.5V | DDR memory |" and "| **HSTL** | 1.2V | DDR3 memory |" — PROBLEM: The DDR mapping is wrong/oversimplified. DDR3 specifically uses the **SSTL_15 (1.5V)** standard, not HSTL; HSTL at 1.2V is associated with DDR4-era/QDR signaling, not DDR3. As written, "DDR3 memory" is attached to the 1.2V HSTL row while the 1.5V SSTL row is labeled only "DDR memory" — the two use-cases are swapped/mismatched. — FIX: Label the SSTL/1.5V row as "DDR3 memory (SSTL15)" and the HSTL/1.2V row as "high-speed / DDR4-class," or simply "QDR/HSTL," rather than calling 1.2V HSTL "DDR3."
- [ ] [LOW] L1190 — QUOTE: "Progress: [█████████░░░░░░░░░░░░░░░░] 36%" — PROBLEM: The ASCII progress bar contains 24 cells (9 filled "█" + 15 empty "░"), which represents 9/24 = 37.5%, but the label says 36% (which corresponds to 9/25, the correct chapter-of-25 fraction). The bar should have 25 cells to match the stated 36%. — FIX: Use a 25-cell bar (9 filled + 16 empty) so the visual matches "36%," or adjust the label to the bar's actual ratio.

## Chapter 10 — FPGA Development (10 issues: 2 HIGH, 5 MED, 3 LOW)

- [x] [HIGH] L806-810 — QUOTE: "always @(posedge clk) begin if (btn_stable && debounce == 0) led <= ~led;" — PROBLEM: The toggle logic is broken in two ways for an active-low button (pressed = 0, released = 1, as the book itself establishes at L508/L523). (a) Wrong polarity: it toggles when `btn_stable` is HIGH (1), i.e. when the button is RELEASED, the opposite of the intended "Toggle LED on button press" (comment L806, prose L783). (b) No edge detection: whenever the button is released and stable, the other always-block sets `debounce <= 0` on every clock (because `btn == btn_stable`), so `btn_stable && debounce == 0` evaluates TRUE on EVERY one of the 27,000,000 clock cycles per second — `led <= ~led` toggles at ~13.5 MHz and the LED just sits half-lit. The code does not do what the section title ("Toggle LED on button press") and prose claim. — FIX: Register the previous value of `btn_stable` and toggle only on the press edge of an active-low button, e.g. `reg btn_prev; ... btn_prev <= btn_stable; if (btn_prev && !btn_stable) led <= ~led;` (falling edge = press). Remove the `debounce == 0` condition entirely.
- [x] [HIGH] L19/L30/L46-47 — QUOTE: "Tang Nano 9K setup - $12 FPGA board!" / "Tang Nano 9K board ($12)" / "Price: $12-15 USD" — PROBLEM: The price contradicts the rest of the book. Chapter 9 (L1010) states explicitly "Tang Nano 9K board-এর দাম আনুমানিক **২,০০০ টাকা ($25)**" and again (L1034) "এই ছোট্ট ২,০০০ টাকার board", and CLAUDE.md lists "~২,০০০ টাকা / $25". Quoting $12 (three times, including "$12-15") roughly halves the figure used everywhere else and confuses the reader about the real cost. The analogy at L42 ("এক বেলা ভালো খাবারের সমান") is also calibrated to the wrong number. — FIX: Make Chapter 10 consistent with Chapter 9 and CLAUDE.md — use ~$25 / ২,০০০ টাকা (e.g. "$20-25 USD") in all three places.
- [ ] [MED] L783 — QUOTE: "যদি signal-টা একটানা কিছুক্ষণ (এখানে ~20ms, একটা counter দিয়ে গোনা) স্থির থাকে" — PROBLEM: The stated debounce window does not match the code. The debounce counter (L793, L801) is 20 bits and waits until `debounce == 20'hFFFFF` = 1,048,575 counts. At 27 MHz that is 1,048,575 / 27,000,000 ≈ 38.8 ms, not "~20ms" — off by roughly a factor of 2. — FIX: Either change the prose to "~39 ms", or change the code threshold to ~540,000 counts (e.g. a comparison giving ≈20 ms) so the number and the code agree.
- [ ] [MED] L938 — QUOTE: "এই `counter[20:0] == 0` শর্তটা একটা সহজ-সরল debounce-এর কাজ করছে ... নইলে একবার টিপলেই pattern গুনে গুনে কয়েকশো বার বদলে যেত!" — PROBLEM: Two errors. (a) It is not debounce — it is a periodic rate-limiter that lets `pattern` advance only when the low 21 bits are zero, i.e. once every 2^21/27e6 ≈ 78 ms; it does nothing to reject the few-ms contact bounce (bounce is far shorter than 78 ms and the gate fires regardless). Holding S1 still advances the pattern ~13 times per second. (b) "কয়েকশো বার" (a few hundred) understates the magnitude by ~4 orders of magnitude: without the gate, a held button advances `pattern` once per clock = 27,000,000 times per second (millions per press), not "hundreds". — FIX: Call it a rate-limiter / slow-sampling gate (or add real edge detection), and correct the figure to "কোটিবার / millions of times per second".
- [ ] [MED] L159 — QUOTE: "**6 × LEDs** | তোমার প্রথম output (RGB × 2, single color × 3)" — PROBLEM: The breakdown does not add up to 6. "RGB × 2" is already 6 LEDs (or 6 color channels), and "single color × 3" adds 3 more, totalling 9 — but the row, the board, and the pin table (L516-524, LED 0–5) all have exactly 6 LEDs. The Tang Nano 9K has 6 single-color LEDs. — FIX: Describe the 6 LEDs accurately, e.g. "6 single-color LEDs" (consistent with the colour labels Blue/Green/Red ×2 in the pin table at L518-522); drop the "RGB × 2, single color × 3" claim.
- [ ] [MED] L162 — QUOTE: "**8 MB PSRAM** | বড় external memory (chip-এর ভেতরের RAM ছোট)" — PROBLEM: Calling the PSRAM "external memory" (and drawing it as a separate board component at L128 "[ 8 MB PSRAM ]") contradicts Chapter 9 (L990), which states explicitly that the GW1NR-9 carries "in-package PSRAM থাকায় নামে বাড়তি 'R'". The PSRAM is in the same package as the FPGA, not a separate board-level chip; "external" misleads about the very feature that gives the 'R' variant its name. — FIX: Call it "in-package PSRAM" (প্যাকেজের ভেতরের PSRAM) to match Chapter 9, and note it is on a separate die inside the same package rather than an external board chip.
- [ ] [MED] L909 — QUOTE: "leds = 6'b000001 << counter[23:21];" — PROBLEM: `counter[23:21]` is a 3-bit value (0–7), but `leds` is only 6 bits wide. For shift values 6 and 7 the single set bit is shifted completely out, so all LEDs go dark for 2 of every 8 steps. The prose (L931) describes this only as "একটা আলো বাঁ থেকে ডানে দৌড়ায়" / "একটা জ্বলন্ত bit-কে শিফট করে সরিয়ে নেওয়া", with no mention that the light vanishes for a quarter of the cycle — the running-light visibly stalls in darkness. — FIX: Mask the shift to 6 states (e.g. shift by a value 0–5, or use a modulo-6 index), or document the intentional dark gap so the behaviour is not surprising.
- [ ] [LOW] L298 — QUOTE: "Family: GW1N-9C" — PROBLEM: The family/series name is missing the 'R'. The device string on the very next line is `GW1NR-LV9QN88PC6/I5` (with 'R'), and Chapter 9 consistently names the part "GW1NR-9C" (L990) / "GW1NR-9" (L1014, L1018). In the Gowin IDE wizard the series for this part is GW1NR-9C. "GW1N-9C" is inconsistent with both the adjacent device string and Chapter 9. — FIX: Change to "Family/Series: GW1NR-9C" for consistency.
- [ ] [LOW] L996-998 — QUOTE: "Logic Elements: 45 / 8640 (0.52%) - LUTs: 42 - Registers: 26" — PROBLEM: The sub-breakdown does not reconcile with the headline. "Logic Elements: 45" sits directly above "LUTs: 42" — 42 ≠ 45, and there is no explanation of what the extra 3 elements are or why LUTs alone don't equal the logic-element count. (The Registers figure 26 = 25-bit counter + 1 led reg is correct, so the discrepancy stands out.) — FIX: Make the numbers consistent (e.g. set Logic Elements = LUTs = 42, or relabel so the headline equals the sum of its parts).
- [ ] [LOW] L477/L481 — QUOTE: "IO_PORT \"clk\" PULL_MODE=UP;" and "IO_PORT \"led\" PULL_MODE=UP DRIVE=8;" — PROBLEM: `PULL_MODE=UP` is pointless and pedagogically misleading on these two pins. The book itself explains (L501, L508) that pull-up matters for floating *inputs* such as buttons; but `clk` is an input actively driven by the 27 MHz crystal oscillator (never floating), and `led` is an *output* — a pull resistor on a driven output does nothing useful. Applying PULL_MODE=UP here, right after teaching that pull-ups are for floating inputs, contradicts that lesson. — FIX: Drop PULL_MODE on the clock and the LED output (keep `DRIVE=8` on the LED), reserving PULL_MODE=UP for the button inputs where the book says it belongs.

## Chapter 11 — FPGA Projects (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [x] [HIGH] L1196-1198, L1223 — QUOTE: "end else begin / // Falling edge - sample data / rx_buffer <= {rx_buffer[6:0], miso};" and prose "যা Mode 0-এর নিয়ম মেনে চলে: ... আর SCLK যখন নামছে (falling edge), দুই দিকেই sample/shift হয় — MISO থেকে আসা bit `rx_buffer` এ ঢোকে" — PROBLEM: The code samples MISO on the FALLING edge of SCLK (the `else` branch runs when `sclk==1`, i.e. transitioning 1→0), but the text explicitly claims this is SPI Mode 0. SPI Mode 0 (CPOL=0, CPHA=0) samples the incoming bit on the LEADING/RISING edge and changes the outgoing bit on the trailing/falling edge. Sampling on the falling edge is CPHA=1 (Mode 1) behaviour, not Mode 0. So the code does not implement the mode the prose claims, and a real Mode-0 slave (e.g. most sensors) would be sampled out of phase. — FIX: Either swap the branches so MISO is sampled in the `if (sclk == 0)` (rising-edge) branch and MOSI is updated on the falling-edge branch, or change the prose/parameters to state this is Mode 1. Keep code and the "Mode 0" claim consistent.
- [x] [HIGH] L611-616, L630 — QUOTE: "hsync <= (h_count >= H_VISIBLE + H_FRONT) && (h_count < H_VISIBLE + H_FRONT + H_SYNC); / vsync <= (v_count >= V_VISIBLE + V_FRONT) && (v_count < V_VISIBLE + V_FRONT + V_SYNC);" — PROBLEM: This drives hsync/vsync HIGH during the sync pulse window, i.e. ACTIVE-HIGH sync. The 640×480@60 Hz VESA mode used throughout this section requires BOTH HSYNC and VSYNC to be ACTIVE-LOW (negative polarity). With the polarity inverted the monitor will not lock and typically shows "Out of Range" / black screen. The follow-up prose praises this exact block ("hsync active হয় শুধু front porch পেরোনোর পর") without flagging the polarity, reinforcing the error. — FIX: Invert both signals, e.g. `hsync <= ~((h_count >= H_VISIBLE+H_FRONT) && (h_count < H_VISIBLE+H_FRONT+H_SYNC));` (and the same for vsync), or assign 0 inside the sync window and 1 elsewhere, and add a note that 640×480@60 uses active-low sync.
- [x] [HIGH] L119-131, L147 — QUOTE: "SEND: begin / if (counter < CYCLES_PER_BIT - 1) begin / counter <= counter + 1; / end else begin / counter <= 0; / bit_index <= bit_index + 1; / tx_data <= {1'b1, tx_data[9:1]}; ..." and the note "এই Quick-Win version-টা সহজ রাখার জন্য `START` state-এ counter দিয়ে অপেক্ষা করে না (তাই START bit-টা পরের গুনতির সাথে মিশে যায়)" — PROBLEM: In the SEND state the output `tx` is NEVER assigned. `tx` is only driven in IDLE (`tx<=1`) and START (`tx<=tx_data[0]`, the start bit). The internal `tx_data` register shifts correctly, but because nothing copies `tx_data[0]` to `tx` during SEND, the line stays stuck at the start-bit value and the actual 8 data bits + stop bit never appear on the wire. The "Quick Win" therefore transmits nothing recognizable. The explanatory note understates this as a mere START-timing simplification, when in fact the data never reaches the pin. — FIX: Add `tx <= tx_data[0];` inside the SEND state (before/after the shift), and correct the note to say the data bits are emitted via `tx <= tx_data[0]` each bit time; the only intended simplification is the START timing.
- [ ] [MED] L1193-1195, L1223 — QUOTE: "if (sclk == 0) begin / // Rising edge - output data / mosi <= tx_buffer[7];" and prose "SCLK যখন উঠছে (rising edge), master নতুন bit MOSI-তে বসায়" — PROBLEM: MOSI is updated on the SAME clock event that drives SCLK from 0→1 (the rising edge). In Mode 0 the slave samples MOSI ON that rising edge, so MOSI must already be stable BEFORE the edge — updating it simultaneously is a setup-time violation; the new bit is presented exactly when it is being latched. — FIX: Drive MOSI on the falling-edge branch (one half-clock before the rising sampling edge), so data is set up ahead of the edge the slave samples on. (Pairs with issue 1.)
- [ ] [MED] L1043, L1015-1026 — QUOTE: "input wire [15:0] number, // 4 digits (hex)" feeding "bcd_to_7seg decoder(.bcd(current_bcd), .segments(segments));" where the decoder is "case (bcd) 4'd0 ... 4'd9 ... default: segments = 8'b00000000;" — PROBLEM: `number` is documented as 4 HEX digits, but each 4-bit nibble is decoded by `bcd_to_7seg`, which only defines 0-9 and sends everything else (4'd10–4'd15 = A–F) to `default`, i.e. a blank digit. So any hex value with A–F nibbles displays blanks, contradicting the "hex" label. — FIX: Either relabel the comment to "4 BCD digits (0-9 only)", or extend the decoder with A–F segment patterns (e.g. A=8'b01110111, b=8'b01111100, C=8'b00111001, d=8'b01011110, E=8'b01111001, F=8'b01110001) to genuinely support hex.
- [ ] [MED] L759, L771, L798 — QUOTE: "reg [24:0] slow_counter;" with comment "// Update brightness every ~1.24 s (when slow_counter wraps)" and prose "25-bit counter ০-এ ফিরে আসে প্রতি `2^25 / 27,000,000 ≈ 1.24` সেকেন্ড পরপর, আর কেবল তখনই ... `brightness` এক ধাপ বদলায়।" — PROBLEM: The math (2^25 / 27e6 ≈ 1.243 s) is correct, but the design consequence is wrong/unstated: brightness changes ONE step per ~1.24 s, and a full breath is 256 steps up + 256 steps down = 512 steps ≈ 512 × 1.24 s ≈ 10.6 MINUTES per cycle. That is nowhere near a perceptible "breathing" effect (which should be ~2-5 s). — FIX: Shrink the slow counter so a full up+down cycle is a few seconds — e.g. `reg [17:0] slow_counter;` (2^18/27e6 ≈ 9.7 ms per step → ~5 s full breath) — and update the "~1.24 s" comment/prose accordingly.
- [ ] [MED] L141 — QUOTE: "তাই প্রতি bit-এ কতগুলো clock cycle অপেক্ষা করতে হবে? `27,000,000 / 9600 = 2812` cycle।" — PROBLEM: 27,000,000 / 9600 = 2812.5, not 2812. The text presents it as an exact equality. The Verilog `localparam CYCLES_PER_BIT = CLK_FREQ / BAUD_RATE` truncates to 2812, introducing ~0.018% per-bit timing error that accumulates over a frame; this rounding is neither shown nor explained, and the "= 2812" reads as exact arithmetic. — FIX: Write "27,000,000 / 9600 = 2812.5, যা integer division-এ 2812 হয়ে যায়" and briefly note the small truncation error (negligible over 10 bits, but real); optionally suggest rounding to 2813.
- [ ] [MED] L872-903 — QUOTE: "module debounce #(...) ( input wire clk, input wire button_in, output reg button_out ); ... reg [19:0] counter; ... always @(posedge clk) begin if (button_out != button_sync_1) begin counter <= counter + 1; ..." — PROBLEM: The `debounce` module has NO reset and NO initial value for `button_out` or `counter`. At power-up `button_out` is X, so the very comparison `button_out != button_sync_1` is undefined and `counter` starts from an unknown value — the module can latch garbage before settling. Every other module in this chapter (uart_tx_complete, uart_rx, pwm, spi_master, i2c_master) has an explicit `reset`, so this is also internally inconsistent, and the surrounding prose stresses robustness/metastability without noting the missing reset. — FIX: Add a `reset` input and `if (reset) begin button_out <= 0; counter <= 0; end` (or at least initialize `button_out`/`counter` to 0), matching the rest of the chapter.
- [ ] [LOW] L93-97, L139 — QUOTE: "localparam IDLE = 2'b00; localparam START = 2'b01; localparam SEND = 2'b10; localparam STOP = 2'b11;" and prose "পুরো ব্যাপারটা একটা ছোট্ট state machine — চারটে অবস্থা: `IDLE`, `START`, `SEND`, `STOP`।" — PROBLEM: The Quick Win FSM declares a `STOP` state and the prose says there are four states, but `STOP` is never entered or used anywhere in the case statement (the stop bit is handled implicitly via the shifted-in 1's during SEND). It is dead code and the "four states" description overstates what the machine actually does. — FIX: Either remove the unused `STOP` localparam and say "তিনটে কার্যকর অবস্থা" (IDLE, START, SEND), or actually use a STOP state for the final bit.
- [ ] [LOW] L1130 — QUOTE: "এই দুটো শুধু ঠিক করে: clock idle অবস্থায় HIGH না LOW থাকবে (CPOL), আর data কোন edge-এ পড়া হবে — উঠন্ত না নামন্ত (CPHA)।" — PROBLEM: The CPHA description is oversimplified/misleading. CPHA does not select "rising vs falling" in absolute terms; it selects whether sampling happens on the LEADING or TRAILING edge of SCLK relative to CPOL. Whether the leading edge is rising or falling depends on CPOL, so the same CPHA value samples on a rising edge for CPOL=0 but a falling edge for CPOL=1. Describing CPHA as simply "উঠন্ত না নামন্ত" conflates it with CPOL and feeds the Mode-0 confusion in issue 1. — FIX: Reword to "data leading edge-এ না trailing edge-এ পড়া হবে (CPHA); leading edge উঠন্ত না নামন্ত সেটা আবার CPOL ঠিক করে।"

## Chapter 12 — Processor Architecture (10 issues: 4 HIGH, 4 MED, 2 LOW)

- [ ] [HIGH] L500-516 vs L481-484 — QUOTE (Fetch Stage): "৪. PC ← PC + 1 (পরের বারের জন্য তৈরি)।" and pseudocode "PC = PC + 1; // আঙুল পরের লাইনে সরাও" — PROBLEM: Section 12.4 plainly states the PC is incremented (PC ← PC+1) inside the **FETCH** stage. But the actual `control_unit` FSM only asserts `pc_write` in the **WRITEBACK** state (`WRITEBACK: begin reg_write = 1; pc_write = 1; end`), and the section 12.3 prose at L395 also says "WRITEBACK state-এ ... PC এগোনোর signal দেয়।" So the book gives two contradictory accounts of when PC advances (FETCH vs WRITEBACK). A learner tracing the cycle cannot reconcile them. — FIX: Pick one model and make it consistent. Since the implemented control unit updates PC in WRITEBACK, change the Fetch Stage description to say PC is only *read/used* for addressing in FETCH and is actually incremented in WRITEBACK; or move the increment narrative to the Writeback Stage. Remove "PC ← PC + 1" from the FETCH step list and the FETCH pseudocode.
- [ ] [HIGH] L347 vs L472-478 — QUOTE: "**`zero`** — ফলাফল কি ঠিক `0`? এটাই `BEQ` (branch if equal)-এর প্রাণ: দুটো সংখ্যা সমান কিনা জানতে হলে বিয়োগ করে দেখো ফল `0` কিনা।" — PROBLEM: The text claims BEQ works by subtracting and testing the zero flag, but the EXECUTE `case (opcode)` in `control_unit` has no entry for BEQ (`4'b1000`); it falls through to `default: alu_op = 3'b000;` which performs ADD, not SUB. The incoming `zero_flag` port is never read, and `pc_src`/branch are never set, so BEQ as described (subtract → check zero → branch) is not realized anywhere in the chapter's control logic. The explanation promises behaviour the design does not perform. — FIX: Either add a BEQ case to the EXECUTE decode (`4'b1000: alu_op = 3'b001; // SUB for compare`) plus branch logic using `zero_flag`, or explicitly tell the reader this is left as an exercise/simplification and that the shown control unit does not yet implement BEQ — so the prose doesn't overstate the current design.
- [ ] [HIGH] L562-565, L575-576, L707 — QUOTE: "LOAD R1, [R2] → Address = R2 (কোন ঠিকানা পড়তে হবে)" and "**LOAD:** memory থেকে data পড়ে আনা → `data = memory[address]`" — PROBLEM: The execution narrative says LOAD/STORE use an address computed from a register (R2 / R0), but in the top-level `simple_processor` the memory address is hard-wired to the program counter: "assign mem_addr = pc;". There is no path from the register file to `mem_addr`, so the processor can only ever address memory at the instruction location, never at the register-supplied data address the LOAD/STORE prose describes. The narrative datapath and the integrated datapath disagree. — FIX: Note in section 12.5 that this simplified top level uses `mem_addr = pc` and therefore does not yet support data-memory addressing for LOAD/STORE (mention it as a known limitation / Day-6 task), or add an address mux (`assign mem_addr = (mem_read|mem_write) ? reg_read2 : pc;`) so the wiring matches the LOAD/STORE explanation.
- [ ] [HIGH] L753-759 (Program 2) and L769-772 (Program 3) — QUOTE: "0x00 | 0001_01_00 | LOAD R1, [R0]" ... "0x02 | 0010_01_00 | STORE R1, [R0]" — PROBLEM: These sample programs depend on LOAD/STORE actually reading/writing data memory at the address held in a register, but per issues 2-3 the shown processor (a) never asserts `mem_write` correctly for STORE in WRITEBACK, (b) addresses memory only via PC, and (c) `reg_write_data` is hard-wired to `alu_result` (L709), so a LOAD can never write `mem_read_data` back into the register — `R1 ← memory_data` from L595 is impossible. Thus the "Result: memory[R0] = 15" and the loop's counter behaviour cannot occur on the design as presented. The programs claim outcomes the chapter's hardware cannot produce. — FIX: Add a writeback mux selecting between ALU result and memory data (`assign reg_write_data = mem_to_reg ? mem_read_data : alu_result;`) and a memory-address mux, OR add an explicit caveat that these programs illustrate the ISA and require the LOAD/STORE datapath completed in the Day-6 build step.
- [ ] [MED] L789-798 vs L494, L569-579 — QUOTE: "তাই আমাদের CPI = 5" with the table summing Fetch+Decode+Execute+Memory+Writeback = ৫ — PROBLEM: The chapter elsewhere says the Memory stage does nothing for non-LOAD/STORE instructions and "না থাকলে কার্যত শূন্য (ধাপটা শুধু পার হয়ে যায়)" (L579) and "ADD-এর মতো instruction এখানে এসে যেন একটা ফাঁকা স্টেশন পার করে এগিয়ে যায়।" But the implemented FSM (L438-447) unconditionally walks FETCH→DECODE→EXECUTE→MEMORY→WRITEBACK for *every* instruction, so the MEMORY state still consumes a clock cycle even for ADD. The CPI table label "Memory (দরকার হলে)" / "(if needed)" implies it is sometimes skipped, contradicting both the fixed CPI=5 claim and the actual FSM. — FIX: Make the wording consistent: state that in this simple FSM every instruction passes through all 5 states so CPI is always exactly 5 (the MEMORY state is idle but still costs a cycle); drop the "দরকার হলে / if needed" qualifier from the CPI table, or compute a true average CPI if some instructions really skip stages.
- [ ] [MED] L894, L902 vs L201, L362-369 — QUOTE: "✅ ALU (8 operations)" and "ALU (8 operations)" in Stats — PROBLEM: The ALU `localparam` list defines 8 ops (ADD, SUB, AND, OR, XOR, SLL, SRL, PASS), so "ALU (8 operations)" is internally true, but it clashes with the ISA, which the same summary lists as "Instructions: 9" while only 6 of the ISA's nine ops are arithmetic/logic (ADD, SUB, AND, OR, plus implied) — and the ISA table (L203-213) never mentions XOR, shift (SLL/SRL), or PASS at all. A reader is told the machine has 9 instructions but the ALU exposes XOR/shift operations that no instruction can ever invoke, and the prose at L341 only lists "যোগ, বিয়োগ, AND, OR, shift" (omitting XOR). The instruction set and ALU capability set are presented inconsistently with no explanation of the gap. — FIX: Either note that the ALU is intentionally over-provisioned (extra ops reserved for later chapters / the Final Project's "16 instructions"), or trim the ALU to the operations the ISA actually uses; and align the L341 prose list ("যোগ, বিয়োগ, AND, OR, shift") with the real op list (add XOR, or say "ইত্যাদি").
- [ ] [MED] L529, L536-538 — QUOTE: "ধরো IR-এ আছে `00110110`, যার মানে `ADD R1, R2`।" with field breakdown "Opcode = 0011 ... RegA=01 ... RegB=10 ... (R1) (R2)" — PROBLEM: This is correct for ADD, but it silently establishes a destination convention that conflicts with the LOAD/STORE convention used later. For ADD the result is written to RegA (R1), per L544 "Register write: R1". Yet for "STORE R1, [R0]" (L754) the *source* of stored data is RegA (R1) while RegB (R0) is the address — i.e., RegA is sometimes a destination, sometimes a data source, and RegB is sometimes a source operand (ADD), sometimes an address (LOAD/STORE). The chapter never states this dual role of the two register fields, leaving the toy instruction format's semantics ambiguous (e.g., for LOAD, is RegA destination and RegB address? the text assumes so but never says it). — FIX: Add one sentence after the bit-field figure defining the field roles per instruction class: for ALU ops RegA = dest+src1, RegB = src2; for LOAD RegA = dest, RegB = address; for STORE RegA = data source, RegB = address. This makes the format self-consistent.
- [ ] [MED] L113 — QUOTE: "তাই DSP (Digital Signal Processor) আর অনেক microcontroller-এ Harvard ব্যবহার করা হয়, যেখানে গতিটা খুব জরুরি।" — PROBLEM: Calque / imprecise causal phrasing. "যেখানে গতিটা খুব জরুরি" ("where speed is very important") is a loose English-style relative clause that reads awkwardly in Bengali and also overstates the reason — microcontrollers commonly use Harvard for deterministic instruction fetch and to keep code in non-volatile flash separate from data RAM, not purely for "speed." The sentence presents only the speed motive. — FIX: Rephrase to natural Bengali and broaden the reason, e.g., "এই কারণেই DSP আর অনেক microcontroller Harvard architecture ব্যবহার করে — যেখানে প্রতি cycle-এ instruction আর data একসাথে পাওয়া দরকার (এবং code ও data আলাদা রাখা সুবিধাজনক)।"
- [ ] [LOW] L905 vs L789-800 — QUOTE: "CPI: 5 cycles" — PROBLEM: Unit/phrasing error. CPI (Clock cycles Per Instruction) is already a count of cycles, so "CPI: 5 cycles" is redundant/incorrect — it should read "CPI: 5" or "5 cycles/instruction". Writing "CPI: 5 cycles" is like writing "speed: 5 km/h per hour." Minor but it's a technical-units slip in a stats box meant to teach the metric correctly. — FIX: Change to "CPI: 5" (or "5 cycles/instruction").
- [ ] [LOW] L771 — QUOTE: "0x02 | 0010_01_11 | STORE R1, [R3] ; Save counter" combined with the counter logic — PROBLEM: Unclear / under-explained loop semantics. Program 3 stores and re-loads the counter from memory every iteration (LOAD at 0x00, STORE at 0x02) even though the counter already lives in R1, which is needlessly confusing for a first loop example and obscures the increment logic; additionally the comment "R2 = 1 (increment)" on the ADD line (L770) describes a *register precondition*, not what the ADD instruction does, mixing setup assumptions into the instruction listing. A beginner cannot tell from the listing alone why the value is round-tripped through memory. — FIX: Either drop the per-iteration LOAD/STORE (keep the counter in R1 and just ADD + compare + JUMP), or add a sentence explaining that the counter is kept in memory deliberately; and move "R2 = 1" / "R0 = 10" purely into the precondition block (L776-778) so the instruction column only describes the instruction's action.

## Chapter 13 — RISC-V Basics (10 issues: 2 HIGH, 5 MED, 3 LOW)

- [ ] [HIGH] L560 / L17,L102 / L979,L997 — QUOTE: "## ১৩.৪ RV32I Instruction Set - Complete" and "✅ Instruction Set - 47 instructions" / "Instructions: 47 base instructions" / "✅ All 47 base instructions" / "Instructions learned: 47" — PROBLEM: The section claims to be the *complete* RV32I set and the number 47 is repeated 4+ times, but the chapter actually lists only 39 instructions (Arith 3, Logical 6, Shift 6, Compare 4, Branch 6, Jump 2, Load 5, Store 3, Upper-imm 2, System 2 = 39). The standard "47" count for RV32I additionally includes the memory-ordering instructions FENCE and FENCE.I (and the CSR instructions CSRRW/CSRRS/CSRRC/CSRRWI/CSRRSI/CSRRCI in the Zicsr group). FENCE is never mentioned anywhere in the chapter, so the "Complete / 47" claim is false against the chapter's own list. — FIX: Either add FENCE/FENCE.I (and note CSR instructions) so the list reaches 47, or change the count to the actual 39 base arithmetic/branch/load/store/system instructions taught and drop the word "Complete". Be consistent across L17, L102, L560, L979, L997.
- [ ] [HIGH] L868 — QUOTE: " addi sp, sp, -4 # Allocate stack" — PROBLEM: This is the chapter's canonical calling-convention example, but it violates the RISC-V psABI requirement that the stack pointer be 16-byte aligned at every call boundary. Decrementing sp by only 4 leaves sp 4-byte aligned, which breaks the ABI the section is specifically teaching (and would crash code that calls libraries assuming 16-byte alignment). L878 ("addi sp, sp, 4") has the same problem. — FIX: Use a 16-byte-aligned frame, e.g. `addi sp, sp, -16` ... `sw x1, 12(sp)` ... `lw x1, 12(sp)` ... `addi sp, sp, 16`, and add a one-line note that RISC-V requires sp to stay 16-byte aligned.
- [ ] [MED] L384 — QUOTE: "উপরের `ADD x5, x6, x7`-কে ডান থেকে বাঁয়ে জোড়া দিলে পুরো ৩২ বিট দাঁড়ায়: `0000000 00111 00110 000 00101 0110011`।" — PROBLEM: The bit string is written MSB-first / left-to-right (funct7 `0000000` first, opcode `0110011` last), which is the normal reading order. Telling the reader to concatenate "ডান থেকে বাঁয়ে" (right to left) contradicts the order in which the string is actually printed and will confuse a beginner assembling the word. (It also slightly conflicts with L331, which correctly says reading the *diagrams* right-to-left makes bit numbers increase.) — FIX: Change to "উপরের field-গুলো বাঁ থেকে ডানে (funct7 → opcode) পরপর সাজালে পুরো ৩২ বিট দাঁড়ায়:" so the described direction matches the printed string.
- [ ] [MED] L85 — QUOTE: "এখানে `G` হলো `IMAFD` একসাথে (general-purpose set), আর `C` হলো compressed।" — PROBLEM: Stated as an equality, this is technically incorrect. The ratified `G` (general) shorthand expands to `IMAFD_Zicsr_Zifencei`, not just `IMAFD`; it also pulls in the control-and-status-register (Zicsr) and instruction-fence (Zifencei) extensions. Presenting `G = IMAFD` exactly leaves out the very CSR/FENCE pieces that issue #1 also shows are missing. — FIX: Write "`G` ≈ `IMAFD` এর সাথে কিছু base সহায়ক extension (Zicsr, Zifencei) — মানে general-purpose set" or at least add "(+ Zicsr, Zifencei)".
- [ ] [MED] L779 — QUOTE: "CALL offset → JAL x1, offset" — PROBLEM: The `call` pseudo-instruction does NOT map to a single `JAL x1`. For an arbitrary far symbol the assembler expands `call` into a two-instruction sequence `AUIPC x1, %pcrel_hi(sym)` + `JALR x1, x1, %pcrel_lo(sym)`; a single `JAL` only reaches ±1MB. Equating `call` to one `JAL x1` is wrong for the general case and is inconsistent with how the chapter itself treats `LI`/`LA` as two-instruction expansions (L774-L775). — FIX: Show it as a (possibly) two-instruction expansion, e.g. "CALL offset → AUIPC x1, hi; JALR x1, x1, lo (কাছের target হলে JAL x1, offset)".
- [ ] [MED] L744 vs L526/L775 — QUOTE: "- PC-relative addressing (AUIPC + offset)" — PROBLEM: "AUIPC + offset" is not a real idiom and contradicts the chapter's own correct statements that the PC-relative address pattern is `AUIPC + ADDI` (L526: "`AUIPC + ADDI` = `LA` pseudo-instruction"; L775: "LA rd, label → AUIPC + ADDI"). AUIPC only produces the upper 20 bits of a PC-relative address; a following ADDI (or load/store/JALR) supplies the low 12 bits. — FIX: Change L744 to "PC-relative addressing (AUIPC + ADDI / load / store)" so it matches L526 and L775.
- [ ] [MED] L620 — QUOTE: "SRLI x5, x6, 3 # x5 = x6 >> 3 (divide by 8)" — PROBLEM: SRLI is a *logical* right shift, so "divide by 8" is only correct for unsigned / non-negative values. For a signed negative number a logical right shift does NOT divide by 8 (it injects 0s into the top bits and produces a large positive result); signed division-by-power-of-two needs the arithmetic shift SRAI. Labeling a logical shift as "divide by 8" right beside the SRA/SRL distinction taught at L624 is misleading. — FIX: Qualify it, e.g. "# x5 = x6 >> 3 (unsigned ÷ 8)"; reserve the plain "÷" framing for SRAI, or add "(ধনাত্মক সংখ্যার জন্য)".
- [ ] [LOW] L201 — QUOTE: "Total: 1024 bits storage" — PROBLEM: Although 32 × 32 = 1024, x0 is hardwired to zero and physically stores nothing (L203/L264: "always zero (hardwired)" / writes discarded), so the actual storage in the register file is 31 × 32 = 992 bits. Stating "1024 bits storage" right next to the statement that x0 is hardwired is internally inconsistent. — FIX: Either say "32 × 32 = 1024 bits (logical; x0 is hardwired, so 31 registers actually store data)" or drop the absolute "storage" figure.
- [ ] [LOW] L543 vs L469 — QUOTE: "Immediate: PC-relative byte offset; imm[0] সবসময় 0 (2-byte aligned)" — PROBLEM: The B/J encodings drop only imm[0] and call targets "2-byte aligned", but L469 correctly states that in pure RV32I every instruction is "৪ বাইট" (4-byte) aligned. In RV32I without the C extension, branch/jump targets are therefore 4-byte aligned, so imm[1] is also always 0 — the "2-byte aligned" justification only becomes necessary once the C (compressed) extension exists, which this chapter explicitly excludes (L88-L92). The two statements are inconsistent for the ISA actually being built. — FIX: Add a half-line noting the encoding reserves only imm[0] for forward-compatibility with the 2-byte-aligned C extension, even though RV32I itself is 4-byte aligned.
- [ ] [LOW] L739-L740 / L480 — QUOTE: "LUI x5, 0x12345 # x5 = 0x12345000" then "AUIPC x5, 0x1000 # x5 = PC + 0x01000000" — PROBLEM: Two minor presentation defects. (a) Placed back-to-back, the LUI line visibly preserves the hex digits (0x12345 → 0x12345**000**), but the AUIPC line shifts 0x1000 into a different column (0x1000 << 12 → 0x0**1000000**), so a beginner who just learned the LUI "append three zeros" rule will be confused by 0x1000 → 0x01000000 (the rule is `imm << 12`, not "append zeros", when imm itself contains low zeros). A worked "0x1000 << 12 = 0x01000000" would remove the confusion. (b) Unrelated formatting nit in the B-type diagram width row (L480): widths are written inconsistently as "1bit"/"4bits"/"1bit" (no space) alongside "7 bits"/"5 bits"/"3 bits" (with space). — FIX: Add the intermediate "0x1000 << 12 = 0x01000000" to the AUIPC example, and normalize the L480 width labels to "1 bit"/"4 bits".

## Chapter 14 — Single-Cycle CPU (10 issues: 4 HIGH, 3 MED, 3 LOW)

- [ ] [HIGH] L1359-1372 — QUOTE: "| **মোট (critical path)** | **~12 ns** |" and "`12 ns` cycle মানে সর্বোচ্চ frequency `1 / 12ns ≈ 83 MHz`" — PROBLEM: The critical path is the load instruction, whose mermaid chain (L1359) is PC → IMEM(~5ns) → RF(~2ns) → ALU(~3ns) → DMEM(~5ns) → WB. Memory is traversed TWICE (instruction fetch + data access), so the sum is 5+2+3+5 = 15ns just for the boxes shown, and with the MUX/routing ~2ns row from the table it is 5+2+3+5+2 = ~17ns. The stated "~12 ns" counts memory only once, so both the total and the derived Fmax are wrong. — FIX: Make the total ~17 ns (IMEM 5 + RF 2 + ALU 3 + DMEM 5 + MUX/routing 2) and update Fmax to 1/17ns ≈ 59 MHz; alternatively split "Memory access (IMEM/DMEM) ~5 ns" into two separate 5 ns rows so the addition is transparent.
- [ ] [HIGH] L356-357 — QUOTE: "দুটো ছোট কিন্তু দামি signal: `zero` (ফল `0` কিনা — branch-এর সমতা যাচাইয়ে কাজে লাগে) আর `negative` (ফলের সবচেয়ে উপরের bit, অর্থাৎ চিহ্ন)।" — PROBLEM: This is factually false for the design actually built in this chapter. The branch decision comes entirely from the separate `branch_comparator` module (L932-937 → `branch_taken`, used at L865 as `branch & branch_taken`). The ALU's `zero`/`negative` outputs are wired to internal wires (L837, L927-928) that are never read anywhere else in `riscv_single_cycle` — they are dead. So `zero` is NOT used for branch equality. — FIX: Either remove the claim that `zero` is used for branch comparison (state plainly that this CPU compares in `branch_comparator`, and `zero`/`negative` are provided for completeness/optional use), or change the datapath to actually derive BEQ/BNE from the ALU `zero` flag.
- [ ] [HIGH] L1213/L1220/L1246/L1274 — QUOTE: "ebreak # Stop" (and "# 00100073 # ebreak") — PROBLEM: Every sample program ends with `ebreak` commented as "Stop", implying the CPU halts. But `ebreak` is opcode `1110011` (SYSTEM), and `control_unit` (L604-668) has NO case for it — it falls into `default` where all signals are 0 (a NOP). Nothing in the datapath halts execution, so after `ebreak` the PC simply advances by 4 and the CPU keeps fetching whatever follows. The "Stop" semantics are never implemented. — FIX: Either add a `halt`/SYSTEM-opcode handler (e.g. a `halt` signal that freezes PC) and describe it, or stop calling `ebreak` a "Stop" — clarify that in this minimal core `ebreak` is decoded as a NOP and the testbench simply stops the simulation after a fixed time.
- [ ] [HIGH] L1373-1374 — QUOTE: "এমনকি সরল `add`, যার আসলে memory দরকারই নেই আর ৬-৭ns-এই হয়ে যেত।" — PROBLEM: The "~6-7 ns" figure for `add` contradicts the chapter's own per-stage delays. An R-type `add` path is IMEM(5) + RF(2) + ALU(3) + MUX/routing(2) = ~12 ns (it only skips the second memory access, DMEM 5 ns). By these numbers `add` would be ~12 ns, not 6-7 ns. — FIX: Use a number consistent with the table, e.g. "`add` only needs IMEM+RF+ALU+MUX ≈ 12 ns and skips the extra ~5 ns of data memory, yet still pays the full ~17 ns load cycle."
- [ ] [MED] L113-116 — QUOTE: "ALU --> RES[\"Result\"]\n RES --> DMEM[\"Data Memory\"]\n DMEM --> WB[\"Write Back\"]\n WB --> RF" — PROBLEM: The bird's-eye overview diagram routes EVERY instruction through Data Memory before writeback (ALU → Result → Data Memory → Write Back → RF). There is no ALU-result-to-writeback bypass, so the diagram implies even `add`/`addi`/`lui`/`jal` write their result through memory, which is wrong and can mislead a beginner. (The detailed diagram at L185-189 correctly shows separate paths.) — FIX: Add a direct ALU-result → Write-Back arrow that bypasses Data Memory, and label the Data-Memory path as load/store-only, so the overview matches the real datapath.
- [ ] [MED] L637 — QUOTE: "branch = 1;\n alu_op = 2'b01; // Subtraction for compare" (with L356-357 / L741) — PROBLEM: The chapter sets `alu_op=01` so the ALU computes a subtraction "for compare" on branches, but that ALU result and its `zero`/`negative` flags are never consumed — the branch_comparator does the real comparison. The narrative presents two contradictory comparison mechanisms (ALU SUB vs dedicated comparator), and the ALU-SUB-for-branch path is vestigial. — FIX: Reconcile the design narrative: either remove `alu_op=01`/the "Subtraction for compare" story and rely solely on `branch_comparator`, or actually use the ALU's SUB/zero result for branches and drop the separate comparator. Don't describe both as active.
- [ ] [MED] L366/L397/L837/L928 — QUOTE: "output wire negative" / "assign negative = result[31];" — PROBLEM: `negative` is generated by the ALU, declared as a top-level wire (L837), and connected (L928), but it is never read by any module or assign in `riscv_single_cycle`. It is a dangling/dead signal, yet the prose (L356) calls these flags "দামি" (valuable). For a teaching design this stray output invites confusion about where it is used. — FIX: Either remove `negative` (and the unused `zero`) from the top-level wiring and note the ALU exposes them for optional use, or wire them into actual logic (e.g. SLT/branch) so the "valuable" description is justified.
- [ ] [LOW] L1297-1342 — QUOTE: "`forever #5 clk = ~clk` প্রতি ৫ একক পর পর clock উল্টে ১০ns period বানায়" — PROBLEM: The testbench has no `` `timescale `` directive, so `#5`/`#10000` are in unspecified simulator time units, not nanoseconds. The text asserts a concrete "10ns period" (and "1000 cycles" at L1324) as fact, which only holds if a timescale of 1ns is assumed somewhere. — FIX: Add `` `timescale 1ns/1ps `` at the top of the testbench (or note the assumed unit), so the "10ns period / 1000 cycles" statements are actually true.
- [ ] [LOW] L102-117 — QUOTE: "flowchart TD\n PC[\"PC (Program Counter)\"] --> IMEM[\"Instruction Memory\"]" (overview diagram) — PROBLEM: The overview diagram has no next-PC feedback edge (no PC+4 / branch / jump arrow returning to PC) and no Immediate-Generator path, even though the surrounding prose (L95-100) explicitly describes "এই একই চক্র প্রতি cycle-এ ঘুরতে থাকে" and immediates. The diagram therefore does not show the loop/cycle it claims to illustrate. — FIX: Add a "next PC (PC+4 / branch / jump)" arrow back into PC and (optionally) an Immediate node feeding the ALU, so the bird's-eye picture reflects the described cyclic datapath.
- [ ] [LOW] L1506 — QUOTE: "Address space: 4GB" — PROBLEM: In the "Stats" summary box this reads as a property of the processor as built, but the implemented system has only a 1KB instruction memory (L1011) and a 4KB data memory (L1177). 4GB is the architectural 32-bit address range, not what this design provides; placed beside concrete build stats (32 registers, 32-bit data) it is misleading. — FIX: Qualify it, e.g. "Address space: 4 GB (architectural, 32-bit); implemented: 1KB IMEM + 4KB DMEM", or move the 4GB figure out of the build-stats box.

## Chapter 15 — Multi-Cycle CPU (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [ ] [HIGH] L089 — QUOTE: "| `ADD` | 12ns (১ cycle) | 3 cycle × 3ns = **9ns** | দ্রুত! single-cycle-এ ৭ns অপচয় হতো |" (and L093-094: "`ADD` ৩ cycle-এই খালাস") — PROBLEM: The intro timing table claims `ADD` (R-type) takes **3 cycles**, but the rest of the chapter consistently states R-type/`ADD` takes **4 cycles**: the CPI table L283 ("R-type … FETCH → DECODE → EXECUTE → WRITEBACK | **4**"), the LW walk-through L1022-1023 ("`ADD` … MEM ধাপ এড়িয়ে — তাই ৪ cycle-এই শেষ"), and the performance table L1044 ("R-type … 4"). The FSM itself routes R-type as FETCH→DECODE→EX_R→WRITEBACK = 4 states. This is a direct internal contradiction that breaks the entire timing example. — FIX: Change to "4 cycle × 3ns = **12ns**" (and update "৩ cycle-এই খালাস" → "৪ cycle-এই খালাস"). Note this makes ADD = single-cycle's 12ns, so the surrounding "দ্রুত!" framing must be reworded too (multi-cycle ADD is not faster here).
- [ ] [HIGH] L204-206 / L452-456 — QUOTE: "এই cycle-এও ALU বসে থাকে না — এটা **আগাম (speculative)** ভাবে branch target (`PC + immediate`) হিসাব করে রাখে" / "DECODE: … alu_src_a = 2'b00; // PC … alu_op = 2'b00; // ADD (for branch target)" — PROBLEM: This is doubly wrong. (a) By DECODE, the PC has already been bumped to PC+4 in FETCH (L448 `pc_write=1`), so `PC + immediate` here computes `(old_pc+4) + imm`, NOT the correct PC-relative target. (b) The actual datapath never uses this DECODE ALU result for branching — `branch_target` is a separate combinational wire `assign branch_target = old_pc + immediate;` (L821), deliberately built from `old_pc`. The DECODE ALU/ALUOut computation is dead for branches and the "speculative branch target" explanation contradicts the code's own `old_pc` design (which the chapter itself stresses at L733-739). — FIX: Remove the "speculative branch target" claim. DECODE only reads rs1/rs2 into A/B and builds the immediate; the branch target is computed separately from `old_pc` in EX_BRANCH/the target mux. Drop the "(for branch target)" comment on the DECODE ALU.
- [ ] [HIGH] L479 / L526 — QUOTE: "alu_op = 2'b01; // SUB for comparison" / "`EX_BRANCH` | … `alu_op=SUB` … | তুলনা (subtraction); শর্ত মিললে PC ← branch target" — PROBLEM: The chapter says the branch condition is decided by the ALU subtraction (zero/negative flags). But in the actual datapath the branch decision comes from a dedicated `branch_comparator` module (L918-923, output `branch_taken`), and PC-write is `pc_write | (pc_write_cond & branch_taken)` (L807). The ALU's `zero`/`negative` outputs (L897-898) are never wired into the branch path, so the EX_BRANCH SUB is dead w.r.t. the actual comparison. The prose mis-explains how the branch is taken. — FIX: State that the comparison is performed by the `branch_comparator` (using funct3 on A/B), not by reading ALU flags; either remove the EX_BRANCH SUB or clarify it is unused/legacy.
- [ ] [MED] L077 — QUOTE: "| Throughput | Low | প্রায়ই বেশি হতে পারে |" — PROBLEM: The summary table claims multi-cycle throughput is "often higher," but the chapter's own worked example and conclusion say the opposite: L1058 "multi-cycle গতিতে single-cycle-কে হারায়নি" (120ns vs 123ns) and L1060 "গতি কখনোই multi-cycle-এর মূল আকর্ষণ ছিল না." Throughput = 1/(time per instruction); with near-equal total time it is roughly equal, not higher. The table overstates the benefit and contradicts §15.7. — FIX: Change to "প্রায় সমান (similar)" or "নির্ভর করে (depends; often ≈ single-cycle)" to match the performance analysis.
- [ ] [MED] L089 — QUOTE: "দ্রুত! single-cycle-এ ৭ns অপচয় হতো" — PROBLEM: The "7ns wasted" figure is unsupported and inconsistent. With single-cycle = 12ns and multi-cycle ADD = 9ns (as the cell states), the difference is 12−9 = **3ns**, not 7ns. A 7ns waste would require ADD's real work to be 5ns, which is never stated anywhere. The number appears arbitrary. (Compounded by issue #1, where ADD should be 12ns/4 cycles, making the "waste" 0.) — FIX: Either drop the "৭ns" figure or replace with the actually-derivable saving and state the assumed ADD work-time explicitly.
- [ ] [MED] L239 — QUOTE: "মোট = 2 + 5 + 1 + 1 = **9 states**।" — PROBLEM: The "2" is mislabeled/unexplained relative to the mapping table just above (L231-237), which lists FETCH and DECODE as two **separate** conceptual stages each with one state. The breakdown should be 1 (FETCH) + 1 (DECODE) + 5 (EXECUTE) + 1 (MEMORY) + 1 (WRITEBACK). Lumping FETCH+DECODE into a "2" with no explanation is inconsistent with the five-stage framing the section is built on. — FIX: Write "1 + 1 + 5 + 1 + 1 = **9 states**" (FETCH + DECODE + 5×EX + MEMORY + WRITEBACK).
- [ ] [MED] L680 / L707 — QUOTE: "`0` → প্রয়োজনে শূন্য (যেমন কোনো মান সরাসরি pass করতে)" / "2'b10: alu_a = 32'h00000000; // Zero" — PROBLEM: The ALU-A "Zero" source (`alu_src_a=2'b10`) is presented as a usable, purposeful input, but the control FSM never selects it — every state sets `alu_src_a` to only `2'b00` (PC) or `2'b01` (A); `2'b10` is never asserted (verified: no `alu_src_a = 2'b10` in the control module). The "যেমন কোনো মান সরাসরি pass করতে" justification describes a use that does not occur. — FIX: Either remove the Zero option from the prose/mux (it is unused), or add the concrete instruction/state that actually needs it; do not present a dead mux input as functional.
- [ ] [LOW] L341 — QUOTE: "input wire [6:0] funct7," — PROBLEM: `multi_cycle_control` declares `funct7` (and `funct3`) as inputs, but neither is referenced anywhere inside the module — next-state logic keys only on `state`/`opcode`, and output logic keys only on `state`/`opcode`. `funct7` is a dead port on the control unit (the real funct decoding happens in `alu_control`). This is dangling/confusing for a learner copying the module. — FIX: Remove the unused `funct3`/`funct7` ports from `multi_cycle_control`, or add a comment that they are reserved/unused here.
- [ ] [LOW] L399-405 / L470 — QUOTE: "EX_LOAD: begin\n next_state = MEMORY;\n end\n \n EX_STORE: begin\n next_state = MEMORY;\n end" (and shared outputs at L470 "EX_LOAD, EX_STORE: begin") — PROBLEM: `EX_STORE` is fully redundant with `EX_LOAD`: identical control outputs (combined in one case branch, L470) and identical next-state (both → MEMORY), with load/store later disambiguated by `opcode` inside MEMORY. The chapter advertises "9 states" as a deliberate design, yet two of them are indistinguishable, slightly undercutting the state-count rationale (could be 8). — FIX: Either merge into a single `EX_ADDR` state (and adjust the "9 states" count/diagram), or add a note explaining why they are kept separate despite being identical.
- [ ] [LOW] L1005 — QUOTE: "অর্থাৎ control আর datapath একে অন্যকে খাওয়ায়: datapath instruction তুলে দেয়, control cycle সাজায়।" — PROBLEM: "একে অন্যকে খাওয়ায়" is a literal calque of English "feed each other," which reads awkwardly/unidiomatically in Bengali (it suggests literal feeding). — FIX: Use natural Bengali such as "একে অন্যকে চালায়/তথ্য জোগায়" or "পরস্পরকে সচল রাখে" (e.g., "control আর datapath পরস্পরকে চালায়: datapath instruction জোগায়, control cycle সাজায়।").

## Chapter 16 — Pipelining (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [ ] [HIGH] L196-205 — QUOTE: "আর হলুদ-চিহ্নিত cycle 5 থেকে cycle 9 পর্যন্ত হলো **steady state**, যেখানে প্রতি cycle-এ ঠিক একটা instruction শেষ হয়" — PROBLEM: In the 5-instruction example table (C1–C9) the pipeline is actually FULL (all five stages busy at once) only during cycle 5. From C6 onward it is DRAINING (4, then 3, 2, 1 instructions in flight). "Steady state" means all stages busy AND one completion per cycle; C6–C9 have a completion but are emptying out, so they are drain, not steady state. This also contradicts the immediately preceding line 195, which correctly says the empty upper-right cells are "drain" — the same C6–C9 columns are simultaneously labeled drain and steady state. With only 5 instructions there is effectively just one steady-state cycle. — FIX: Either describe C5 alone as the (single) full/steady-state cycle and C6–C9 as drain, or use a longer instruction stream (e.g. 8–9 instructions) so a genuine multi-cycle steady-state band exists; do not call the draining cycles steady state.
- [ ] [HIGH] L1363-1368 — QUOTE: "IF: ADD SUB AND OR XOR / ID: ADD SUB AND OR XOR / EX: ADD SUB AND OR XOR / MEM: ADD SUB AND OR / WB: ADD SUB AND" — PROBLEM: This ASCII "Stage usage" block contradicts the authoritative table directly above it (L1352-1358), which correctly shows every one of the five instructions (ADD SUB AND OR XOR) passing through every stage. Here IF/ID/EX list all 5 instructions but MEM lists only 4 (drops XOR) and WB lists only 3 (drops OR and XOR). Every instruction must traverse MEM and WB, so MEM and WB each process all five; the truncation is a rendering error, made worse because the three upper rows in the same block DO show all five. — FIX: Complete the rows to "MEM: ADD SUB AND OR XOR" and "WB: ADD SUB AND OR XOR" (shifting each one column further right as in the table), so the ASCII matches the table and the "All stages busy!" caption.
- [ ] [HIGH] L793, L1077-1099, L1122, L1138 — QUOTE: "output wire lui" (id_stage port, L793) … `.lui_in(1'b0),` (id_ex_register instantiation, L1122) — PROBLEM: The narrative (L342-347, L388-391) stresses that ALL control signals must travel down the pipeline with the instruction. But `lui` is broken at every hop: id_stage declares a `lui` output yet the id_stage instantiation (L1077-1099) never connects `.lui(...)`; the ID/EX register's `lui_in` is hard-wired to `1'b0` (L1122) instead of `id_lui`; and the register's `lui_out` (declared L477) is left unconnected in the instantiation (it stops at `.funct3_out`, L1138). So LUI control is silently dropped — a real inconsistency between the prose ("control signal-ও বহন করে") and the wiring, and a latent functional gap for LUI/AUIPC. — FIX: Either remove `lui` entirely from id_stage/id_ex_register if LUI is out of scope for this ideal chapter (and say so), or wire it through: declare `id_lui`, add `.lui(id_lui)` to the id_stage instance, set `.lui_in(id_lui)`, and connect `.lui_out(...)` to an `ex_lui` that the EX stage actually uses.
- [ ] [MED] L88-89, L94-95 and L99-103 — QUOTE: "এক বোঝা কাপড় পুরো শেষ করে তবেই পরের বোঝা machine-এ দিলে dryer অর্ধেক সময় খালি বসে থাকে" — PROBLEM: The laundry example has THREE sequential stages (washing machine → dryer → folding). When loads are run fully sequentially, the dryer is active only 1 of every 3 time units per load, i.e. it sits idle about TWO-THIRDS of the time, not "অর্ধেক" (half). The "half idle" figure would only be right for a two-stage pipeline. — FIX: Change "অর্ধেক সময় খালি বসে থাকে" to "বেশিরভাগ সময় খালি বসে থাকে" or "প্রায় দুই-তৃতীয়াংশ সময় খালি বসে থাকে" to match the three-stage example.
- [ ] [MED] L239-241 and L258-261 — QUOTE: "General formula: / Speedup = (Pipeline depth) × (Efficiency) / Ideal 5-stage: 5×" and "efficiency হলো pipeline কতটা ভরা থাকছে (আদর্শে ১, বাস্তবে hazard-এর কারণে একটু কম)" — PROBLEM: As stated, the formula cannot reproduce the chapter's own 4.8× ideal result. With depth=5 and "efficiency = 1 in the ideal, hazard-free case" (which is exactly the case being computed), Speedup × Efficiency = 5×1 = 5×, not 4.8×. The 4.8× shortfall here comes purely from the startup FILL latency for a finite 100-instruction run, which this formula does not model — the text explicitly reserves "efficiency" for hazards, so fill is double-uncounted. The correct finite-stream relation is Speedup = depth / (1 + (depth−1)/n). — FIX: Note that the depth×efficiency formula is the asymptotic (n→∞) limit where fill is negligible, and present the finite-n formula separately for the 100-instruction case so 4.8× is actually derivable; or define efficiency to include fill so the numbers close.
- [ ] [MED] L156-159 and L288-291 — QUOTE: "Single-cycle-এ যে এক লম্বা ক্লকে পাঁচটা কাজই করতে হতো, সেটাকে পাঁচ ভাগে কেটে পাঁচগুণ দ্রুত ক্লক চালানোই pipeline-এর মূল চাল" (L157-159) — PROBLEM: The clock period is set by the SLOWEST stage, not by depth÷5, and the chapter itself says so later (L288-290: "ক্লককে শুধু সবচেয়ে ধীর একটা stage-এর সমান লম্বা হলেই চলে"). The blanket "পাঁচ ভাগে কেটে পাঁচগুণ দ্রুত ক্লক" at L157-159 asserts an exact 5× clock with no caveat, then L156 separately claims the five stages are "মোটামুটি সমান কাজের ভাগে কাটা" — real RV32I stages are notably unequal (MEM/IF dominated by memory). The unqualified 5× clock claim early on is contradicted by the more careful treatment at L291-293. — FIX: At L157-159 add the same caveat used at L291-293 ("stage-গুলো একদম সমান হয় না, register-ও বিলম্ব যোগ করে, তাই লাভ ঠিক পাঁচগুণ নয়"), so the early "পাঁচগুণ দ্রুত ক্লক" claim is consistent with the later one.
- [ ] [MED] L862-863 and L549-551 — QUOTE: "branch ভুল হলে এর পরের দুটো instruction-কে বাতিল করতে হবে — সেটাই উপরের `flush` logic-এর কাজ" — PROBLEM: "উপরের flush logic" points the reader upward, but in reading order the flush wiring (`if_id_flush = ex_branch_taken`, `id_ex_flush = ex_branch_taken`) appears far BELOW, at L1242-1243 in §16.4, not above the EX-stage code. The only nearby "above" mention of flush is the EX/MEM comment at L549-551, which says the EX/MEM register deliberately has NO flush — so "উপরের flush logic" actively points at the one register that is NOT flushed, which is misleading. — FIX: Change to a forward reference, e.g. "নিচে top-level module-এ `if_id_flush`/`id_ex_flush` logic (§১৬.৪)" so it points to the actual flush assignments at L1242-1243.
- [ ] [LOW] L162-167 vs L1324-1339 — QUOTE: "Cycle: 1 2 3 4 5 6 7 8" (L171) versus "Time (cycles) → / 0 1 2 3 4 5 6 7 8" (L1331-1332) — PROBLEM: The two main space-time diagrams use inconsistent cycle numbering: the first/primary diagram and its table (L171, L199 "C1…C9") count cycles starting at 1, while the later identical-style diagram (L1332) and its table (L1352 "C0…C8", and prose L1349 "cycle 4-এ … 0-থেকে-গোনা") count from 0. The same "first instruction finishes" event is "cycle 5" in one scheme and "cycle 4" in the other, which is confusing for a chapter that repeatedly cross-references these diagrams. — FIX: Pick one convention (1-based is already dominant) and renumber the L1331-1358 diagram/table/prose to match, or add an explicit note that the second diagram is 0-indexed.
- [ ] [LOW] L304 and L306-308 — QUOTE: "| 20–31 stages | Intel Pentium 4 (Northwood 20, Prescott 31) | অতিরিক্ত গভীর — too deep! |" then "Pentium 4-এর Prescott-কোর ৩১ stage পর্যন্ত গিয়েছিল … তাই Intel পরে আবার অগভীর design-এ ফিরে আসে" — PROBLEM: Slightly imprecise/overstated history. The widely cited Prescott figure is its branch-misprediction pipeline of ~31 stages; the commonly quoted Northwood depth is ~20 stages. Stating these as flat "20" and "31" stage counts without the "misprediction pipeline / pipeline-depth metric" qualifier presents disputed marketing-era numbers as exact fact in a teaching table. — FIX: Soften to "প্রায় ২০" / "প্রায় ৩১ (branch-misprediction pipeline)" and note these are approximate, depending on how stages are counted.
- [ ] [LOW] L1-2 and L1488, L1497 — QUOTE: "## From Sequential to Parallel - 5× Performance Boost!" (L2) vs "Speedup: 4.8× (ideal)" (L1497) — PROBLEM: The chapter title promises a flat "5× Performance Boost", but the body is careful and explicit that (a) latency does not improve at all, (b) the ideal finite-run speedup is 4.8× (L237, L1284, L1497), and (c) real speedup with hazards is only ~3–4× (L1311, L1315). The unqualified "5×" headline (repeated L17, L181) slightly oversells relative to the chapter's own honest numbers. — FIX: Either qualify the headline ("up to ~5×" / "near-5× throughput") or keep "5×" only when paired with the word "throughput/ideal", to stay consistent with the 4.8× / 3–4× figures the chapter itself derives.

## Chapter 17 — Hazards & Forwarding (10 issues: 3 HIGH, 3 MED, 4 LOW)

- [ ] [HIGH] L835-841 — QUOTE: "| Data hazards | +0.1–0.3 | ... | Load-use stalls | +0.2–0.5 | ... | Branch mispredictions | +0.5–1.0 | ... | **Typical মোট** | **CPI = 1.3–1.5** |" — PROBLEM: The component rows do not add up to the stated total. Summing onto Base 1.0 gives a minimum of 1.0+0.1+0.2+0.5 = 1.8 and a maximum of 1.0+0.3+0.5+1.0 = 2.8, i.e. a CPI range of 1.8–2.8. The stated "Typical মোট CPI = 1.3–1.5" is impossible — even the lowest possible sum (1.8) already exceeds the stated maximum (1.5). It also contradicts the worked example in the same section (134 cycles / 100 instr ≈ 1.34). — FIX: Make the rows consistent with the 1.3–1.5 total — e.g. Data hazards +0.0 (forwarding is free), Load-use stalls +0.1–0.2, Branch mispredictions +0.2–0.3, so Base 1.0 + ~0.3–0.5 = 1.3–1.5; or relabel the total as a worst-case bound.
- [ ] [HIGH] L852 — QUOTE: "Single-cycle হলে লাগত: 100 instruction × 5 = 500 cycle-সমতুল্য কাজ" — PROBLEM: A single-cycle processor executes each instruction in exactly ONE clock cycle (CPI = 1), so 100 instructions take 100 cycles, not 500. The "×5" belongs to a MULTI-cycle CPU (~5 cycles/instruction). The real advantage of pipelining over single-cycle is the much shorter clock PERIOD (≈ one stage instead of all five), not the cycle count — so the comparison must be made in time, not "cycles". Calling it "500 cycle" directly contradicts the term "single-cycle". — FIX: Either compare against a multi-cycle CPU ("multi-cycle হলে ~৫ cycle/instruction"), or frame it explicitly as time: single-cycle clock period ≈ 5× the pipelined period, so 100 instr × (5 time-units) ≈ 500 time-units vs 134 pipelined cycles. Drop the word "single-cycle" or stop calling the result "cycle".
- [ ] [HIGH] L838 — QUOTE: "| Data hazards | +0.1–0.3 | forwarding বেশিরভাগ সারায়, তাই কম |" — PROBLEM: The chapter repeatedly and emphatically states that forwarding costs ZERO cycles: L497 "(কোনো bubble নেই — বিনামূল্যে!)" and L501 "forwarding — যেটা একদম **বিনামূল্যে** hazard সারায়, কোনো cycle নষ্ট না করেই". A data hazard that forwarding resolves therefore adds 0 to CPI. The only data hazard that costs cycles is the load-use hazard, which is already a separate row ("Load-use stalls +0.2–0.5"). So a non-zero "+0.1–0.3" for forwarding-resolved "Data hazards" contradicts the book's own model; its own "কেন" cell ("forwarding বেশিরভাগ সারায়") implies the cost should be ~0. — FIX: Set the Data-hazards contribution to +0.0 (or merge it into the load-use row), keeping consistency with the "forwarding is free" statements.
- [ ] [MED] L40 — QUOTE: "বিরল — RISC-V-এ instruction ও data memory আলাদা" — PROBLEM: Separate instruction and data memory is a Harvard / microarchitecture (implementation) choice, NOT something mandated by the RISC-V ISA. RISC-V defines a single byte-addressable address space; nothing in the ISA requires split I/D memories. Attributing the split to "RISC-V" is a technical-precision error and teaches a wrong reason for why the structural hazard is avoided. — FIX: Reword to attribute it to the design/architecture, e.g. "বিরল — আমাদের নকশায় (Harvard-style) instruction ও data memory আলাদা" rather than to RISC-V.
- [ ] [MED] L483 / L327 — QUOTE: "ভুল পথে শুধু তার চেয়ে **ছোট** (younger) দুটো instruction: একটা IF/ID-তে, একটা ID/EX-এ।" (also L327 "তার পিছনে দুটো instruction (IF/ID আর ID/EX-এ) ঢুকে গেছে") — PROBLEM: This conflicts with the chapter's own cycle-tables. When the branch is resolving in EX (cycle 3), the cycle-tables show the two wrong-path instructions occupying the IF and ID stages: L453-454 "BEQ+4 ... [ID] [XX]" and "BEQ+8 ... [IF] [XX]", and L425-426 say BEQ+8 is "(cycle 3-এ IF)" and BEQ+4 is "(cycle 2-এ IF)". A learner reading "BEQ+8 is in IF" (diagram) but "the wrong-path pair is in IF/ID and ID/EX" (prose) cannot reconcile the two. The prose silently switches to "which register's next write is flushed" framing without saying so. — FIX: Make the framing uniform: either describe both instructions by the stage they occupy at resolution (IF and ID), or clearly state "the two registers whose next contents are flushed are IF/ID and ID/EX" — and note that BEQ+4 sits in ID (IF/ID reg) and BEQ+8 in IF at the resolving cycle.
- [ ] [MED] L133, L607 — QUOTE: "input wire ex_reg_write," — PROBLEM: `ex_reg_write` is declared as an input port of `hazard_detection_unit` and is wired up at instantiation (L607 ".ex_reg_write(ex_reg_write)"), but it is never read anywhere inside the module body (L146-165 use ex_mem_read, ex_rd, mem_reg_write, wb_reg_write, etc., but not ex_reg_write). It is a dead/unused port — confusing because the reader assumes load-use detection might gate on it (it does not; it gates only on ex_mem_read). — FIX: Either remove the unused port and its connection, or actually use it in the load-use condition if intended; at minimum add a comment that it is currently unused.
- [ ] [LOW] L89 — QUOTE: "| **MEM Hazard** (এক ধাপ পুরোনো) | দুই আগেরটা, এখন MEM/WB-এ | ..." — PROBLEM: The parenthetical "এক ধাপ পুরোনো" (one step older) sits beside "দুই আগেরটা" (two instructions before) in the same row, describing the same case with two different distances. A learner cannot tell whether the producer is one or two instructions back. (It is two instructions before; "এক ধাপ" only makes sense if it means "one pipeline stage further along than the EX-hazard case".) — FIX: Use one consistent measure, e.g. "(এক pipeline-stage বেশি পুরোনো result)" or just "দুই-আগের instruction-এর result", and avoid pairing "এক ধাপ" with "দুই আগেরটা".
- [ ] [LOW] L990, L942 — QUOTE: "$display(\"CPI: %.2f\", cycles / 100.0);" — PROBLEM: The testbench hard-codes the instruction count as 100 (and the stall-rate/CPI math depends on it), but the actual test program is never defined — `instruction_memory imem` (L942) is instantiated with no shown contents/`$readmemh`. If the program is not exactly 100 instructions, the printed CPI and "Stall Rate" are meaningless. — FIX: Either load and show a concrete program with a known instruction count, or compute the divisor from a retired-instruction counter instead of the literal 100.
- [ ] [LOW] L88 — QUOTE: "| **EX Hazard** (ALU→ALU) | ঠিক আগেরটা, এখন EX/MEM-এ | EX/MEM থেকে forward (`2'b10`) | ALU result তৈরি, শুধু forward করলেই হয় |" — PROBLEM: The label "(ALU→ALU)" is narrower than what the implemented forwarding actually does. The EX/MEM→EX path also forwards into branch comparisons (L309 branch_comp gets forwarded_rs1/rs2) and store data (L317 store_data = forwarded_rs2), and the producer need not be an ALU op (any reg-writing, already-past-EX instruction qualifies). Calling it strictly "ALU→ALU" understates and can mislead. — FIX: Relax the label, e.g. "(ঠিক-আগের non-load result → EX)" and note the consumer may be an ALU op, a branch, or a store.
- [ ] [LOW] L17 vs L873/L881/L1060 — QUOTE: "✅ Branch Prediction - reduce penalty" (in the opening "এই Chapter-এ তুমি বানাবে" list) — PROBLEM: The opening promises branch prediction as a built deliverable, but the body treats it as not-actually-built: §17.7 is titled "Advanced: Branch Prediction" (L873), the 2-bit predictor is explicitly deferred ("তোমার final project-এর জন্য তোলা রইল", L881), the build plan marks it "Branch prediction (optional)" (L1060), and it appears only under "Final Project" (L1123). The deliverable checklist and the body disagree on whether branch prediction is part of this chapter. — FIX: Mark it "(optional / advanced)" in the opening list too, or move it out of the "তুমি বানাবে" deliverables to match §17.7 and the build plan.

## Chapter 18 — Memory Hierarchy (10 issues: 5 HIGH, 3 MED, 2 LOW)

- [ ] [HIGH] L309/L383/L367 — QUOTE: "FETCH: begin / if (mem_ready) begin / data[index] <= mem_read_data; ... read_data <= mem_read_data; ... state <= IDLE;" (cache_direct_mapped) — PROBLEM: On a WRITE miss the simple cache jumps to FETCH (L367-372) but FETCH (L380-391) only loads `mem_read_data` and never applies the original `write_data`, and never writes it to memory. The write is silently LOST on a write miss. (The module also never latches `write_data`, so it could not apply it 100 cycles later even if it tried.) The prose at L270/L401-403 describes write-through but never mentions this gap, and the advanced controller (L577-580) explicitly handles the same case via `saved_write_data`, proving this is a defect, not an intentional simplification. — FIX: In the simple cache, latch `write_data`/`write_enable` on the miss, and in FETCH apply the write (`if (saved_write) data[index] <= saved_write_data;` plus write-through to memory) before returning, mirroring the controller.
- [ ] [HIGH] L471-475/L573-574 — QUOTE: "wire [9:0] index = cpu_address[11:2]; wire [19:0] addr_tag = cpu_address[31:12]; ... // (FETCH) data[index] <= mem_read_data; tag[index] <= addr_tag;" — PROBLEM: `index`/`addr_tag`/`tag[index]`/`data[index]`/`valid[index]` are all derived from the LIVE `cpu_address`, not from the latched `saved_address`. The whole reason for latching `saved_address` (L521) is that `cpu_address` can change during the multi-cycle miss — yet COMPARE/WRITE_BACK/FETCH still index the arrays with the live address. If `cpu_address` changes, the fetched line is written to the WRONG cache line. Note FETCH already (correctly) uses `saved_address[31:2]` for `mem_address` at L569 but inconsistently uses live `index`/`addr_tag` for the array writes. The prose at L599 wrongly praises the latching as making this correct. — FIX: Derive `index`/`addr_tag` from `saved_address` (e.g. `saved_index = saved_address[11:2]`, `saved_tag = saved_address[31:12]`) and use those everywhere after IDLE.
- [ ] [HIGH] L676/L701/L471 — QUOTE: "cache_controller #(.NUM_LINES(512)) icache(...)" with "wire [9:0] index = cpu_address[11:2];" and "reg valid [0:NUM_LINES-1];" — PROBLEM: `NUM_LINES` is a fake parameter. The address decode is hardcoded for 1024 lines (`index = cpu_address[11:2]` = 10 bits → 0..1023, `tag = address[31:12]` = 20 bits). Instantiating with `NUM_LINES(512)` shrinks the arrays to `[0:511]`, but `index` still ranges 0..1023, so half of all accesses go OUT OF BOUNDS, and the tag width is wrong (512 lines needs 9 index bits + 21 tag bits). The prose at L760 presents 512 as a clean working configuration. — FIX: Parameterize the decode from NUM_LINES, e.g. `localparam IDX = $clog2(NUM_LINES);` with `index = cpu_address[1+IDX:2]` and `tag = cpu_address[31:2+IDX]`, sizing `tag`/array widths accordingly — or keep NUM_LINES=1024 in the instantiations.
- [ ] [HIGH] L690/L715/L724-728 — QUOTE: ".mem_ready(mem_ready)" (on BOTH icache and dcache) with arbiter "wire dmem_access = dmem_read || dmem_write; assign mem_address = dmem_access ? dmem_address : imem_address; ... assign mem_write = dmem_write;" — PROBLEM: The single `mem_ready` from main_memory is broadcast to BOTH caches simultaneously, but the arbiter never gates it to the cache that currently owns the port. When data holds the port and `mem_ready` pulses, an icache that is also waiting in FETCH/WRITE_BACK will ALSO latch `mem_read_data` (which belongs to the data access) → instruction corruption. The comment at L722-723 ("The instruction cache must stall while data has the port") and prose L626 claim this is handled, but no stall/ready-gating exists; the declared `imem_ready`/`dmem_ready` wires (L673,L699) are never used. — FIX: Gate ready per owner, e.g. `assign dmem_ready_q = dmem_access & mem_ready; assign imem_ready_q = ~dmem_access & mem_ready;` and feed those to each cache's `.mem_ready(...)`.
- [ ] [HIGH] L126/L153 — QUOTE: "miss হলে শুধু একটা byte নয়, পুরো একটা **line** (পাশের data সহ) টেনে আনে।" / "Cache একবারে একটা গোটা line আনে ... পাশের data ও সাথে আনাই বুদ্ধিমানের কাজ" — PROBLEM: The chapter's central spatial-locality claim is false for this cache. `LINE_SIZE = 4` bytes = exactly one 32-bit word (`data` is `[31:0]`, one word per line). A miss therefore fetches ONLY the single requested word — there is NO neighboring data brought in, so this cache captures essentially zero spatial locality across words. This directly contradicts the chapter's repeated motivation. — FIX: Either make the line genuinely multi-word (e.g. LINE_SIZE=16/32 bytes, with offset selecting the word and a real burst fill), or correct the prose to state that this teaching cache uses a 1-word line and thus exploits temporal (not spatial) locality.
- [ ] [MED] L203/L304 — QUOTE: "**Offset (নিচের ২ bit):** line-এর ভেতর কোন byte? ... ৪টা byte-কে আলাদা করতে লাগে ঠিক ২ bit" / "wire [1:0] offset = address[1:0];" — PROBLEM: The 2-bit `offset` is computed but never used: reads/writes return/replace the whole 32-bit word (`read_data <= data[index]`, `data[index] <= write_data`), and `mem_address` is formed as `{address[31:2], 2'b00}`, discarding the offset entirely. With a 1-word line and word-granular data, the byte offset is dead logic, contradicting the explanation that it selects a byte within the line. — FIX: Remove the offset from this word-granular design (and say so), or implement actual byte/sub-word selection that uses `offset`.
- [ ] [MED] L416-418/L481 — QUOTE: "Controller-এর FSM — পাঁচটা state" / "লক্ষ করো state এবার আর তিনটা নয়, পাঁচটা" / "localparam ALLOCATE = 3'b010;" — PROBLEM: The prose insists the FSM has FIVE states, but only four are actually used (IDLE, COMPARE, WRITE_BACK, FETCH). `ALLOCATE` (3'b010) is declared and never referenced anywhere in the `case` (confirmed: single occurrence in the file). So "পাঁচটা state" is wrong — it is four functional states plus one dead localparam. — FIX: Either remove the unused `ALLOCATE` localparam and change the text to "চারটা state", or actually implement an ALLOCATE state.
- [ ] [MED] L555-560 — QUOTE: "WRITE_BACK: begin / mem_address <= {tag[index], index, 2'b00}; mem_write_data <= data[index]; mem_write <= 1; if (mem_ready) begin ..." — PROBLEM: `mem_address`/`mem_write_data`/`mem_write` are assigned with nonblocking `<=`, so they update only on the NEXT clock edge, yet `mem_ready` is sampled in the SAME cycle. On the first WRITE_BACK cycle main_memory has not even seen `mem_write` (still its old value), so the handshake is timing-fragile/off-by-one and depends on main_memory ignoring an undriven strobe. Same pattern in FETCH (L569-571). With `mem_ready` also un-gated (see #4), a stale `mem_ready=1` left from a previous op can make WRITE_BACK/FETCH fall through in one cycle. — FIX: Use a clear request/ack handshake (drive `mem_write`/`mem_read` in one cycle, wait for a freshly-asserted `mem_ready` in subsequent cycles; clear strobe on ack), and ensure `mem_ready` is per-transaction.
- [ ] [LOW] L974/L994/L1010 — QUOTE: "wire [31:0] data_address, data_write, data_read; ... .data_read(data_read_enable), ... .data_read_out(data_read), ... // pipeline: .data_read(data_read)," — PROBLEM: Confusing/colliding naming. The wire `data_read` is the read-DATA bus, but the port name `.data_read` means the read STROBE on `memory_system` (fed `data_read_enable`) yet means the read-DATA input on the pipeline (fed `data_read`). The same port identifier `.data_read` carries opposite meanings on two modules, a real readability trap for a teaching text. — FIX: Rename the bus to `data_read_data`/`data_rdata` and keep `.data_read` strictly as the strobe everywhere.
- [ ] [LOW] L1192-1193/L1184 — QUOTE: "Cache size: 4 KB / Cache lines: 1024" under "তুমি বানিয়েছো: ... Dual cache (I$ + D$)" — PROBLEM: The summary Stats claim the built system is 4 KB / 1024 lines, but the integrated dual-cache system actually built in §18.4 uses `NUM_LINES(512)` per cache (≈2 KB data each, 4 KB combined, 512 lines each). The "1024 lines / 4 KB" figures describe only the standalone §18.2 cache, not the dual-cache system the same summary says you built — an internal inconsistency (compare L760 which correctly states 512). — FIX: State the dual-cache figures (e.g. "2 × 512-line caches, 4 KB total"), or reconcile the instantiations and the stats to one consistent size.

## Chapter 19 — Complete System / SoC (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [ ] [HIGH] L264/L1326/L1040 — QUOTE: "parameter CLOCK_FREQ = 50000000, // 50 MHz" (L264) vs "input wire clk, // 27 MHz onboard" (L1326) — PROBLEM: The `uart` module hardcodes a 50 MHz default for `CLOCK_FREQ`, but the FPGA target (Tang Nano 9K) runs at 27 MHz, and the SoC instantiates `uart uart_inst(...)` (L1040) WITHOUT overriding the parameter. So `BAUD_DIV = 50000000/115200 = 434`, but at the real 27 MHz clock the actual line rate becomes 27000000/434 ≈ 62,200 baud instead of 115200. Section 19.8 explicitly promises "USB-তে terminal খুললে \"Hello, World!\" দেখা যাবে" (L1310) — but with this mismatch the terminal output is garbled. This is a genuine functional bug for the headline deliverable. — FIX: Either change the default to `parameter CLOCK_FREQ = 27000000` (and the comment to "27 MHz"), or override it at instantiation: `uart #(.CLOCK_FREQ(27000000)) uart_inst (...)`. Make all three sites (module default, prose, and top-level clock) agree on 27 MHz.
- [ ] [HIGH] L114/L126/L165-L170/L186-L187 — QUOTE: "ROM[\"Boot ROM<br/>(Instructions)\"]" and "BUS <--> ROM" (L114, L126); table "`0x00000000 – 0x00003FFF` | 16 KB | **Boot ROM** | যে program চলবে (instructions)" (L186) — PROBLEM: Both block diagrams and the address map present a unified bus-connected memory with a Boot ROM at 0x0–0x3FFF and RAM at 0x4000–0xFFFF, both hanging off the bus. But the actual SoC (L991-994, L1096-1104) is Harvard-style: instructions come from a separate `instruction_memory imem` fetched directly by the CPU's PC (NOT through the bus), and the bus's entire memory region (`address < 0x1000_0000`, L882) routes only to a single `data_memory dmem`. There is NO ROM instance on the bus at all, so "BUS <--> ROM" is fiction and the "Boot ROM" address range is not a real bus device. Architecture-vs-diagram/map contradiction. — FIX: Reconcile the model. Either add a real bus-connected ROM (decode 0x0–0x3FFF → ROM, 0x4000–0xFFFF → RAM) to match the diagrams, or change the diagrams/address map to show the implemented Harvard split (separate instruction memory off the PC; data RAM on the bus) and stop drawing `BUS <--> ROM`.
- [ ] [HIGH] L1343/L1318 — QUOTE: "assign led = gpio_out[5:0];" (L1343) with "খেয়াল করো `reset = !btn_reset` — কারণ বোর্ডের বোতাম **active-low** (চাপলে ০)।" (L1318) — PROBLEM: The text correctly inverts the active-low button (`reset = !btn_reset`) but drives the Tang Nano 9K LEDs directly with no inversion. The 6 onboard LEDs on the Tang Nano 9K are active-LOW (lit when driven 0). The blink demo sets `*gpio_data = 0x1` to mean "LED on" (L1241), which through `gpio_out = gpio_data & gpio_dir` produces a logic 1 and therefore turns the active-low LED OFF — the demo behaves inverted on real hardware. Inconsistent treatment within the same top module (button inversion handled, LED inversion ignored). — FIX: Invert the LED outputs for this board, e.g. `assign led = ~gpio_out[5:0];`, and add a one-line note that the Tang Nano 9K LEDs are active-low (matching the existing button note).
- [ ] [MED] L1006/L990/L1102 — QUOTE: ".data_funct3(cpu_data_funct3)," (L1006) and "data_memory dmem( ... .mem_size(3'b010), ... )" (L1102) — PROBLEM: The CPU's `data_funct3` is captured into the `cpu_data_funct3` wire (declared L990) but then never used; the data memory's `mem_size` is hardwired to `3'b010` (word). This means every access to the RAM region is forced to a 32-bit word, so byte/halfword loads and stores (LB/LBU/LH/LHU/SB/SH) to data RAM are silently wrong. The prose claims a full "COMPLETE WORKING COMPUTER" (L199) and the Hello-World assembly even uses `lb` (L1172, L1197) — load-byte semantics matter. — FIX: Wire the CPU's funct3 through to memory: `.mem_size(cpu_data_funct3)` (and route it via the bus to the memory side), or explicitly state in the text that this SoC only supports word-aligned 32-bit RAM accesses and that sub-word access is out of scope.
- [ ] [MED] L1495 — QUOTE: "Modules: 6 (CPU + 4 peripherals + bus)" — PROBLEM: The SoC actually instantiates eight modules: `instruction_memory imem`, `riscv_pipelined_with_hazards cpu`, `system_bus bus`, `uart`, `gpio`, `timer`, `interrupt_controller intc`, and `data_memory dmem` (L991-1104). The stat counts only 6 and omits BOTH memory modules, even though "memory" is repeatedly presented as a core part of the SoC ("CPU + memory + চারটা peripheral + bus", L75; "Memory (ROM + RAM)", L84). Undercount / inconsistency. — FIX: State "Modules: 8 (CPU + 4 peripherals + bus + 2 memories)" or rephrase the parenthetical so the memory blocks are not silently dropped.
- [ ] [MED] L1426 — QUOTE: "□ Bus arbiter" — PROBLEM: The Week-2 build plan lists "Bus arbiter" as a deliverable, but no arbiter is ever designed, implemented, or discussed. The `system_bus` (L854-918) does only address decoding + a read-data mux, and the SoC has a single bus master (the CPU), so arbitration is neither present nor needed. Listing a component that is never built (and using "arbiter" where the bus actually does decoding) misleads the reader. — FIX: Replace "□ Bus arbiter" with "□ Address decoder + read-data mux" (the work that is actually done), or add a real multi-master arbiter and a section describing it. Align the term with the single-master design.
- [ ] [MED] L559/L620-L630 — QUOTE: "prescaler আগে clock-কে \"ভাগ\" করে ধীর করে দেয় — যেমন প্রতি ১০০০ clock-এ একবার counter বাড়াও" — PROBLEM: The prose says setting the prescaler to 1000 increments the counter "every 1000 clocks," but the code increments `counter` only when `prescaler_count >= prescaler` and counts `prescaler_count` through 0,1,…,prescaler before resetting (L620-630). With `prescaler = 1000` that is 1001 clocks per counter tick, not 1000. To actually divide by 1000 you must write 999. Off-by-one between explanation and implementation; this propagates into any timing the reader computes. — FIX: Either change the prose to "প্রতি (prescaler+1) clock-এ একবার" (and note that for a divide-by-1000 you load 999), or change the compare in code to `prescaler_count >= prescaler - 1` so the divide ratio matches the stated value.
- [ ] [LOW] L727/L782/L722 — QUOTE: "localparam INT_PRIORITY = 2'b10; // R/W: Priority (not used in simple)" (L727) and table "`+0x8` (`0x10003008`) | **PRIORITY** | R/W | priority (এই সরল ভার্সনে ব্যবহৃত নয়)" (L782) — PROBLEM: PRIORITY is marked "R/W," but it is neither writable (the write `case` at L743-746 handles only INT_ENABLE and INT_ACK) nor readable (the read `case` at L767-771 has no PRIORITY arm, so it returns 0 via `default`). The backing array `reg [7:0] interrupt_priority [0:7];` (L722) is declared and never read or written anywhere — dead code that will draw synthesis warnings. Labeling a non-functional register "R/W" is inconsistent with its own "not used" note. — FIX: Change the Access column to "—" (or "reserved") for PRIORITY, and either remove the unused `interrupt_priority` array or add a short note that it is a placeholder for a future priority-programming feature.
- [ ] [LOW] L964/L1022 — QUOTE: "wire cpu_ready;" (L964) and ".ready(cpu_ready)," (L1022) — PROBLEM: The bus drives a `ready` output into `cpu_ready`, but `cpu_ready` is never consumed: the CPU is instantiated with `.cache_stall(1'b0)` (L1000) and has no stall input tied to the bus, so the bus "ready/not-ready" handshake (L911-916) has no effect — `cpu_ready` is a dangling net. The comments justify it ("single-cycle memory: never stalls", "combinational RAM is always ready"), so it is benign, but the bus's `ready` logic is presented as meaningful (L849-851) while being dead in this integration. — FIX: Either connect `cpu_ready` to a CPU stall input (so wait-states actually work), or add a one-line note that `ready` is provided for future variable-latency devices but is unused here because all current targets respond in one cycle.
- [ ] [LOW] L1497 — QUOTE: "Address space: 256 MB" — PROBLEM: The figure conflates the decoded memory window with real memory. The bus decodes `address < 0x1000_0000` (256 MB) to the memory side, but the address map defines only 16 KB ROM + 48 KB RAM = 64 KB of actual storage (L165-187), and the peripherals add just 16 KB at 0x1000_0000–0x1000_3FFF. Stating a flat "Address space: 256 MB" overstates what exists and may mislead a beginner into thinking 256 MB of memory is present. — FIX: Clarify, e.g., "Decoded memory window: 256 MB (only 64 KB implemented: 16 KB ROM + 48 KB RAM); peripherals: 16 KB at 0x1000_0000," or simply report the total addressable map span rather than an unqualified "256 MB."

## Chapter 20 — Advanced Topics (10 issues: 2 HIGH, 4 MED, 4 LOW)

- [ ] [HIGH] L496-500 — QUOTE: "ADD v1, v2, v3 # Add 4 values at once!\n\nv1 = [a, b, c, d]\nv2 = [e, f, g, h]\nv3 = [a+e, b+f, c+g, d+h]" — PROBLEM: Destination/source registers are inconsistent. This chapter uses destination-first assembly everywhere else (L124 "ADD, SUB…", L204 "ADD x1, x2, x3" then L206 "ADD x6, x1, x7 # Independent!" — x1 is written then read, confirming dest-first; L229-232 same). Under that convention `ADD v1, v2, v3` writes v1 = v2 + v3, i.e. v1 should hold [e+a, f+b, …]. But the example declares v1 and v2 as the *inputs* and shows the *result* landing in v3. The instruction's destination (v1) and the register receiving the sum (v3) do not match. — FIX: Make it consistent, e.g. operands v2=[a,b,c,d], v3=[e,f,g,h] and result `v1 = [a+e, b+f, c+g, d+h]`; or rewrite the mnemonic as `ADD v3, v1, v2`.
- [ ] [HIGH] L254-261 — QUOTE: mermaid edges "RS --> E1[\"Execute\"] … E1 --> RET[\"<b>Retire (in order)</b><br/>result commit করা\"]" — PROBLEM: The data flow goes Reservation Stations → Execute → Retire, with the ROB sitting only on the front-end path (FD --> ROB --> RS). This bypasses the ROB on the completion/writeback path. But the accompanying text insists the ROB "program order ধরে রাখে" and enables in-order retire and rollback (L298-301). For in-order commit to work, finished results must write back *into the ROB* and then commit from the ROB head — they cannot flow straight from the execution units to "Retire." The diagram therefore contradicts the mechanism the text describes. — FIX: Route execution results back to the ROB (e.g. E1..E4 --> ROB, and ROB --> RET), so retirement happens from the ROB in program order.
- [ ] [MED] L329-330 — QUOTE: "Pipeline depth: 15-20 stages\nMispredict penalty: 15-20 cycles!" — PROBLEM: This equates total pipeline depth with the misprediction penalty, which is imprecise. The mispredict penalty equals the number of stages between *fetch* and *branch resolution*, not the full pipeline depth (which also includes the MEM/WB/commit stages that come *after* the branch resolves). For a 20-stage pipeline the branch typically resolves before the last stage, so the real penalty is usually somewhat less than 20. Presenting "depth = penalty" as an identity is misleading. — FIX: Clarify, e.g. "branch resolution-এর আগে ~১৫-২০টা stage, তাই mispredict penalty প্রায় ১৫-২০ cycle" (penalty ≈ stages up to resolution, not necessarily the full depth).
- [ ] [MED] L271-272 — QUOTE: "খেয়াল করো নীল box দুটো (Fetch/Decode আর Retire) ক্রম মেনে চলে, কমলা আর বেগুনি অংশটুকুই কেবল \"out of order\"" — PROBLEM: Per the diagram's classDef, the orange ("ooo") boxes are the ROB and the Reservation Stations. Calling the ROB part of the "out of order" portion is conceptually wrong/muddy: the ROB's entire purpose is to *restore* program order so retirement is in-order (the text itself says so at L298-301). Only the reservation stations + execution actually behave out-of-order; the ROB is an in-order structure that brackets them. — FIX: Say only the reservation stations and execution units run out of order, and note the ROB exists precisely to re-impose program order (it is not itself "out of order").
- [ ] [MED] L149-156 vs L176 — QUOTE (diagram): "Q --> A1[\"ALU 1\"] … Q --> A2[\"ALU 2\"] … Q --> LD[\"Load unit\"] … Q --> ST[\"Store unit\"]"; QUOTE (table): "**Multiple Execution Units** (2-4 ALU, 2 Load/Store, Branch unit, FPU)" — PROBLEM: The Superscalar architecture diagram shows only 2 ALUs + 1 Load + 1 Store unit and omits the Branch unit and FPU that the Requirements table on the very next screen explicitly lists as needed. A reader comparing the two sees a mismatch in what units a superscalar core has. — FIX: Add Branch unit and FPU to the diagram (or note it is a simplified 4-wide example showing only a representative subset of units).
- [ ] [MED] L1081 vs L1150 (and project metadata) — QUOTE: "Lines of code: 3000+" (L1081) vs "SO FAR: 460+ KB | 21,000+ lines" (L1150) — PROBLEM: Inconsistent line counts in the same chapter, and the "3000+" lines-of-code figure also undercuts the project-wide stated total of "5,000+ lines of Verilog code" (CLAUDE.md, Total Content). The two boxes use "lines" to mean different things without saying so, and the 3000+ value is internally inconsistent with the book's own headline figure. — FIX: Disambiguate the metrics (e.g. "Verilog code: 5,000+ lines" vs "book content: 21,000+ lines") and reconcile the 3000+/5,000+ discrepancy.
- [ ] [LOW] L392-396 — QUOTE: "| TTTT | 90% taken |\n| TNTN | 95% taken |\n| NTNT | 90% not taken |" — PROBLEM: The percentages are arbitrary and unexplained, and the pattern semantics are undefined — it is never stated which end of "TNTN" is the most-recent outcome. As written, "TNTN → 95% taken" while "NTNT → 90% not taken" only makes sense under a specific (unstated) recency/ordering assumption, so the example is confusing rather than illustrative. — FIX: State the convention (e.g. rightmost = most recent) and give a one-line reason for the prediction, or drop the made-up percentages.
- [ ] [LOW] L130 vs L252 — QUOTE: "issue width = 4, অর্থাৎ প্রতি cycle-এ সর্বোচ্চ ৪টা instruction execution unit-এ পাঠানো যায়" (L130-131) vs RS node "operand-এর জন্য অপেক্ষা … ready হলেই issue" (L252) — PROBLEM: "issue" is used with two different meanings. At L130 it means dispatch from the front-end into the machine ("issue width"); at L252 it is reused for firing a *ready* instruction from the reservation station to an execution unit (which is usually called "dispatch" or "fire/wakeup-select"). Reusing "issue" for both blurs the OoO terminology. — FIX: Use "ready হলেই execute-এ পাঠানো (dispatch/fire)" at L252 to distinguish it from front-end issue.
- [ ] [LOW] L797-798 — QUOTE: "Chapter ২০.৪-এর সেই scalar loop এখানে hardware-এর সমান্তরালতায় গলে গেছে" — PROBLEM: ২০.৪ is a *section* within this chapter, not a separate chapter, so prefixing it with "Chapter" is incorrect/confusing (it reads as a reference to a different chapter). — FIX: Write "Section ২০.৪" / "২০.৪ অংশের" instead of "Chapter ২০.৪".
- [ ] [LOW] L1-2 vs L1146 (and book structure) — QUOTE: "Chapter 20: Advanced Topics & Future of Computing" (L1) and "PART 5: VLSI & SILICON ⏳ IN PROGRESS" (L1146) — PROBLEM: Chapter 20's actual content is advanced *processor architecture* (superscalar, OoO, branch prediction, SIMD, multicore, GPU) — i.e. Part 4 ("Processor Design") material — yet the book's part structure places Chapters 20-25 in "Part 5: VLSI & Chip Fabrication," and the progress box (L1146) shows Part 5 already in progress at Ch20. So an architecture chapter is filed under the VLSI/fabrication part, creating a topic-vs-placement mismatch. — FIX: Either label Ch20 as the closing chapter of the Processor-Design part, or add a sentence clarifying that Ch20 is a bridge chapter (advanced architecture) before the VLSI part proper begins at Ch21.

## Chapter 21 — VLSI Design Flow (10 issues: 3 HIGH, 4 MED, 3 LOW)

- [ ] [HIGH] L287-L297 vs Chapter_23_Sky130_PDK.md L176-L210 — QUOTE: "M6[\"Metal 6 (সবার উপরে) — Power\"]" with stack M6 → M5 → M4 → M3 → M2 → M1 — PROBLEM: This diagram gives Sky130 **six** metal layers (Metal1–Metal6, Metal6 = Power). Sky130 has only **five** routing metal layers. The book's own dedicated PDK chapter says so explicitly and repeatedly: "Sky130-তে মোট **৫টি routing metal layer** (Metal1 থেকে Metal5)" (Ch23 L200) and lists exactly Metal1–Metal5 with widths (Ch23 L206-210, L459-462), with Metal5 (1600nm) as the thick power/long-route layer. So Ch21 both states a factually wrong layer count and directly contradicts Ch23 (a reader who studies both chapters gets M1–M6 here, M1–M5 there). The omission of Sky130's distinctive `li1` (local-interconnect) layer below Metal1 — which Ch23 L181/L193-197 stresses — compounds the mismatch. — FIX: Redraw the stack as Metal5 → Metal4 → Metal3 → Metal2 → Metal1 → (li1) → Poly → Diffusion → Substrate, label Metal5 as the power/long-route layer, and align wording with Ch23 ("৫টি routing metal layer").
- [ ] [HIGH] L412-L414 vs L426-L427 — QUOTE: "Setup Time: ... Path delay < Clock period - Setup time" then four lines later "যদি logic-এ ৮ns লাগে, clock period > ৮ns হতেই হবে / সর্বোচ্চ frequency = 1/8ns = 125 MHz" — PROBLEM: The two statements contradict each other. The setup inequality it just taught rearranges to `Clock period > Path delay + Setup time`, i.e. for 8ns of logic the period must exceed 8ns **plus** the setup time (and clk-to-Q), so it is strictly greater than 8ns. But the worked number then drops setup time entirely and sets period = exactly 8ns to get fmax = 1/8ns = 125 MHz. By the chapter's own formula 125 MHz is unreachable — the true fmax is below it. This teaches the wrong mental model (that fmax = 1/logic_delay) one paragraph after giving the correct constraint. — FIX: Make the example obey the formula, e.g. "logic delay 8ns + setup ~1ns + clk-to-Q ~0.5ns → period must be ≥ ~9.5ns, so fmax is a bit under 125 MHz"; or, if the simplification is intentional, state "setup/clk-to-Q উপেক্ষা করে আদর্শ হিসেবে" so the contradiction is acknowledged.
- [ ] [HIGH] L719-L722 — QUOTE: "OL --> Y --> OR --> MG --> NG --> KL\n OR --> RP\n OR --> TR\n OR --> ST" — PROBLEM: This tool diagram misrepresents the flow ordering and contradicts the chapter's own stage diagram (L110-L121, where order is Synthesis → Floorplan → Placement → CTS → Routing → **STA** → Physical Verification → GDSII). Here (a) **OpenSTA (timing)**, **RePlAce (placement)**, and **TritonRoute (routing)** are drawn as dead-end leaves branching off OpenROAD with no edge back into the flow, so structurally they appear to happen *after* DRC/LVS (MG → NG) rather than inside Place & Route and before signoff; (b) the main signoff chain `OR --> MG --> NG --> KL` bypasses timing entirely, implying STA is optional/off-path; (c) **CTS** has no tool node at all even though it is step 5 in the stage diagram and a core OpenROAD task. The figure caption claims it shows "কোন টুল flow-এর কোন কাজটা করে" (which tool does which job in the flow) — but the edges encode an order that is wrong. — FIX: Make the spine reflect real order, e.g. `Y(Synth) --> OR` and inside OR: Floorplan → RePlAce(Place) → CTS → TritonRoute(Route) → OpenSTA(Timing), then `OR --> MG(DRC/Extract) --> NG(LVS) --> KL`. Add a CTS node (OpenROAD/TritonCTS). Don't leave RP/TR/ST as unconnected leaves.
- [ ] [MED] L177-L194 vs Chapter_23_Sky130_PDK.md L258-L261 — QUOTE: "ভাবো এটা LEGO-র একটা বাক্স: ভেতরে আগে থেকেই **নানা আকারের** block বানানো আছে" and the "Standard Cell কী?" box which lists only Layout/Timing/Power/Area — PROBLEM: The section that first introduces the term "standard cell" never states the property that makes a cell *standard*: a **fixed/uniform height** with **power rails (VPWR/VGND) along top and bottom**, which is exactly what lets automatic place-&-route lay them in rows. Ch23 L258-261 makes this the headline point ("library-র প্রতিটা cell-এর **উচ্চতা এক** ... এই সমান উচ্চতাই automatic place & route-কে সম্ভব করে"). Worse, the Ch21 LEGO analogy says blocks are "নানা আকারের" (various sizes), which actively undercuts the real rule (only **width** varies; height is constant). A learner finishes 21.2 without the single defining characteristic. — FIX: Add one or two sentences: standard cells share a fixed height and have power/ground rails top & bottom so they snap into rows; only width varies by complexity. Adjust the LEGO analogy to "একই উচ্চতার, কিন্তু ভিন্ন চওড়ার block" to match Ch23.
- [ ] [MED] L679 — QUOTE: "✅ counter.def (placement — কোথায় কী বসল)" — PROBLEM: Mislabels the artifact. The DEF emitted at the *end* of the OpenLane flow (this list is "flow শেষ হওয়ার পর", L677) contains the full physical state — floorplan, placement, CTS buffers, **and routing** — not placement alone. Calling `counter.def` the "placement (কোথায় কী বসল)" file tells the reader DEF = placement only, which is wrong and also obscures where routing output lives. The parallel labels are precise for the others (synth → netlist, layout → GDSII), so this one stands out as inaccurate. — FIX: Relabel, e.g. "counter.def (চূড়ান্ত physical layout — placement + routing সহ)" or note that DEF holds the placed-and-routed design.
- [ ] [MED] L112 — QUOTE: "৩. Floorplanning<br/>chip-এর আকার, power grid, IO pad" — PROBLEM: Floorplanning is introduced in the master flow diagram as step 3 and explicitly credited with building the **power grid** (PDN) and **IO pads**, but the chapter never has a Floorplanning section and never explains the power grid / power distribution network anywhere — sections jump from Synthesis (21.2) and design rules (21.3) straight to Placement & Routing (21.4). The one term ("power grid") that the diagram raises is left completely undefined, and floorplanning (arguably the stage that sets die size and where everything else depends) gets no prose at all, unlike Synthesis, Placement, Routing, STA, DRC, LVS, Antenna, and GDSII which each get a subsection. — FIX: Either add a short Floorplanning paragraph (die/core area, IO pad ring, power grid/PDN rails feeding the standard-cell rows) before 21.4, or, if floorplanning is deferred to Ch22, drop "power grid" from the box and add a one-line forward reference so an undefined term isn't dangling.
- [ ] [MED] L536-L539 — QUOTE: "সমাধান:\n- Diode যোগ করা\n- লম্বা তার ভেঙে দেওয়া\n- Multi-level routing" — PROBLEM: "Multi-level routing" is not a standard antenna-fix term and is imprecise/misleading. The real third mitigation is **layer hopping / jogging up to a higher metal** (breaking the long same-layer wire by routing part of it on an upper layer via vias, so the gate is never connected to an over-length antenna during fabrication) — sometimes called "metal jumping" or adding "antenna jumpers." "Multi-level routing" reads like generic multi-metal routing (which every chip uses) rather than a deliberate antenna remedy, so a reader cannot map it to an actual fix. — FIX: Replace with the correct term, e.g. "তারটিকে উপরের metal layer-এ তুলে দেওয়া (layer jumping / antenna jumper)" — i.e. break the long lower-layer run by jumping to a higher layer via vias.
- [ ] [LOW] L191-L194 — QUOTE: "✅ Flip-flop (DFF)" ... "✅ Adder, Latch" — PROBLEM: Listing the **latch** as a routine "প্রচলিত" standard cell alongside the DFF, with no qualification, is misleading in an automatic synthesis/PnR context: latches are level-sensitive, generally avoided in synchronous flows, complicate STA, and "inferred latch" is usually a *bug* the toolchain warns about (the book's own design philosophy is edge-triggered DFF-based). Presenting it flatly next to DFF suggests latches are an ordinary first-choice element. — FIX: Drop Latch from the common-cells list, or qualify it (e.g. "Latch — বিশেষ ক্ষেত্রে, সাধারণত এড়ানো হয়") so beginners don't treat latch == flip-flop.
- [ ] [LOW] L662-L669 — QUOTE: "set ::env(DESIGN_NAME) counter\nset ::env(VERILOG_FILES) counter.v\nset ::env(CLOCK_PERIOD) 10\nset ::env(CLOCK_PORT) clk\n\n# Run flow\nflow.tcl -design counter" — PROBLEM: Mixes two different OpenLane configuration mechanisms in one snippet, which is confusing and not how the tool is actually driven. The `set ::env(...)` lines belong in a design's `config.tcl` (or `config.json`), which `flow.tcl -design counter` then reads from `designs/counter/`. Setting them inline in the same shell before `flow.tcl -design counter` does not feed them into the run (the `-design` form loads the design's own config file), so the snippet as written suggests the inline vars configure the flow when they don't. Inconsistent with Ch22's config-file approach. — FIX: Show one mechanism: put the `set ::env(...)` block in `designs/counter/config.tcl` and then run `flow.tcl -design counter`; or use the explicit-config invocation. Add a one-line note that these settings live in the design's config file.
- [ ] [LOW] L590-L598 — QUOTE: "Layer 64/20: Metal1\nLayer 65/20: Via1\nLayer 66/20: Metal2\nLayer 67/20: Via2\nLayer 68/20: Metal3" — PROBLEM: Although hedged with "(example)", these GDSII layer/datatype numbers do not match real Sky130 and the sequence is internally implausible (it implies one consecutive integer per layer with a constant datatype 20, including for vias). In actual Sky130 GDS, met1 = 68/20, met2 = 69/20, met3 = 70/20, and vias use a *different datatype* (e.g. 68/44, 69/44), not 65/20 / 67/20. Sitting two sections after a chapter that gives concrete real Sky130 numbers (Ch23 metal widths/areas), a curious reader who opens KLayout will find these don't correspond to anything, undercutting the otherwise good point that "প্রতিটা layer = fab-এ আলাদা একটা mask". — FIX: Use representative real Sky130 GDS numbers (met1 68/20, via 68/44, met2 69/20, via2 69/44, met3 70/20 …) or, if keeping it generic, say "(আসল সংখ্যা ভিন্ন; এগুলো শুধু ধারণার জন্য)" and note that vias use a different datatype than metals.

## Chapter 22 — OpenLane Physical Design (10 issues: 4 HIGH, 4 MED, 2 LOW)

- [ ] [HIGH] L944 vs L929 — QUOTE: "| IPC | ~0.8 (with stalls) |" (L944) together with "| Pipeline | Single-cycle (educational) |" (L929). — PROBLEM: A single-cycle processor completes every instruction in exactly one clock cycle and has no pipeline, so it cannot stall and its IPC is by definition 1.0. "IPC ~0.8 (with stalls)" is mutually contradictory with "Single-cycle": stalls and IPC < 1 only arise in multi-cycle or pipelined designs with hazards/memory waits. The datasheet describes the chip as single-cycle, so the IPC figure is technically impossible. — FIX: For a single-cycle core state "IPC | 1.0 (single-cycle)" and drop "(with stalls)"; or, if the design really stalls (e.g., on memory), change the Pipeline row to "Multi-cycle" so the two rows stop contradicting each other.
- [ ] [HIGH] L574, L579 — QUOTE: "Max frequency: 55.2 MHz" appearing inside the output of `cat .../reports/synthesis/1-synthesis.stat.rpt`, described as "**Max frequency** হলো synthesis-এর প্রাথমিক অনুমান, এখানে ৫৫.২ MHz". — PROBLEM: The file `1-synthesis.stat.rpt` is Yosys's `stat` output and contains only chip area and cell/flip-flop/gate counts — it does NOT report a max frequency. Yosys elaboration synthesis performs no timing analysis, so no "Max frequency" line is produced at this stage; the first real frequency/slack number comes from STA after synthesis (the separate sta report). Presenting "Max frequency: 55.2 MHz" as part of the .stat.rpt output is fabricated and teaches the reader to expect a field that isn't there. — FIX: Remove the "Max frequency: 55.2 MHz" line from the .stat.rpt example output (and the sentence reading it), or relocate that claim to a post-synthesis STA report (e.g. reports/synthesis/*sta*) and label it as STA-derived, not from the stat report.
- [ ] [HIGH] L571 vs L928/L935-936 — QUOTE: "Number of flip-flops: 250" (L571), while the datasheet specifies "Registers | 16 × 32-bit" (L928) plus "Instruction | 512 bytes on-chip" / "Data | 512 bytes on-chip" (L935-936). — PROBLEM: A 16 × 32-bit register file alone requires ~480-512 flip-flops (15×32 = 480 if x0 is hardwired, or 16×32 = 512), and a 32-bit PC adds 32 more — already ~512 flip-flops before any memory or control state. If the 512-byte instruction and data memories are also synthesized as flip-flops (no SRAM macro mentioned), the count would be in the thousands. The stated 250 flip-flops is less than half of what the register file by itself needs, so the number is internally inconsistent with the design described. — FIX: Make the flip-flop count consistent with the architecture — e.g. raise it to a figure ≥ register-file size (~500+ just for 16×32 regs + PC), or clarify that memories are implemented as macros and recompute the register-only flip-flop count accordingly.
- [ ] [HIGH] L389-391, L366 — QUOTE: "এরপর প্রতিটা `run_*` command উপরের flowchart-এর একটা করে ধাপ" (L390) and "এটা হুবহু উপরের flowchart-এরই ক্রম" (L366). — PROBLEM: The interactive list (L376-384) has nine commands — run_synthesis, run_floorplan, run_placement, run_cts, run_routing, run_magic, run_magic_spice_export, run_lvs, run_antenna_check — but the flowchart (L292-321) has only seven stages and its step 7 is "Verification: DRC/LVS". The claim that each run_* maps one-to-one to a flowchart step is false: `run_magic_spice_export` and `run_antenna_check` have no flowchart counterpart, and conversely the flowchart's DRC has no `run_drc` command in the list (DRC runs inside run_magic). Calling the order "হুবহু" (exactly) the same as the flowchart is therefore inaccurate. — FIX: Soften the claim ("ধাপগুলো মোটামুটি flowchart-এর ক্রম অনুসরণ করে, সাথে কিছু বাড়তি signoff step যোগ হয়") and either add the antenna-check / spice-export / DRC steps to the flowchart or explicitly note they are extra signoff steps not shown in the diagram.
- [ ] [MED] L943-945 — QUOTE: "| Clock | 40-50 MHz |" / "| IPC | ~0.8 (with stalls) |" / "| MIPS | ~35 |". — PROBLEM: MIPS = frequency(MHz) × IPC. The "~35 MIPS" figure is only self-consistent with the (erroneous) IPC ~0.8 (40-50 × 0.8 = 32-40). Once IPC is corrected to 1.0 for a single-cycle core (see issue 1), MIPS at 40-50 MHz becomes 40-50, not ~35. So the MIPS row silently depends on the wrong IPC and will become inconsistent the moment IPC is fixed. — FIX: After correcting IPC, recompute MIPS = clock × IPC (e.g. ~40-50 MIPS for a single-cycle core at 40-50 MHz), or pin the row to a single clock value so the arithmetic is checkable.
- [ ] [MED] L943 vs L499/L471 — QUOTE: "| Clock | 40-50 MHz |" (datasheet, L943) versus the processor config "set ::env(CLOCK_PERIOD) \"20.0\"" → "২০ ns cycle = ৫০ MHz target" (L471/L499) and the timing example "Worst slack: -0.23 ns (VIOLATED!)" (L598). — PROBLEM: The design is configured and run at a 50 MHz target, but the signoff timing example shows a negative worst slack (VIOLATED), meaning 50 MHz is NOT met on that run; the fix offered is to relax to 40 MHz (L685). The datasheet then reports "Clock | 40-50 MHz" as if both ends were achieved, which conflicts with the worked example where 50 MHz fails timing. A datasheet should quote the achieved (passing) frequency, not the failed target. — FIX: State the achieved clock as a single passing number (e.g. "40 MHz (timing-clean)") consistent with the slack/optimization narrative, instead of the "40-50 MHz" range that includes the failing target.
- [ ] [MED] L383-384 — QUOTE: "run_lvs\nrun_antenna_check" (LVS listed before the antenna check, at the very end of the interactive sequence). — PROBLEM: The antenna check is a routing-related physical check; in the OpenLane interactive flow it is normally run right after routing (before/around GDS streamout), not after LVS. Placing `run_antenna_check` as the final step after `run_lvs` misrepresents the conventional signoff order and would have the reader run a routing check after schematic comparison. — FIX: Reorder so the antenna check follows routing (e.g. run_routing → run_antenna_check → run_magic → run_magic_spice_export → run_lvs), matching OpenLane's documented interactive sequence.
- [ ] [MED] L961, L984-986 — QUOTE: "| Package | QFN-64 (TinyTapeout standard) |" (L961) and "TinyTapeout-এ সব chip এই একই মান package-এ আসে; এটাই তাদের standard" (L984-985). — PROBLEM: In TinyTapeout it is the entire multi-project die (hundreds of users' designs together) that is packaged in a single QFN — not each individual user design. Saying "সব chip এই একই package-এ আসে" implies each student's small design becomes its own QFN-64 part, which is wrong: the user's design is one tile on a shared chip. The claim "তোমার design মাত্র ২০টা pin ব্যবহার করছে বলে ৬৪-pin package-এ দিব্যি আঁটছে" (L985-986) compounds this — a TT user design does not own the package pins; IO is muxed/shared, not a direct 20-of-64 pin mapping. — FIX: Reword to make clear the QFN-64 packages the whole multi-project TinyTapeout chip and that individual designs occupy a tile with a shared/muxed IO interface, not their own pins.
- [ ] [LOW] L199, L216 — QUOTE: "set ::env(CLOCK_PORT) \"\"" with the explanation "inverter-এ clock নেই, তাই খালি (`\"\"`)". — PROBLEM: For a purely combinational design OpenLane still expects timing setup; leaving CLOCK_PORT as an empty string is fragile and version-dependent — many OpenLane versions emit warnings or fail to create a proper clock/virtual clock, and the more robust documented approach for clockless designs is to omit CLOCK_PORT (or use a virtual clock) rather than set it to "". Teaching `""` as the canonical way risks confusing errors on the reader's first run. — FIX: Either note that for a combinational design CLOCK_PORT may simply be omitted (and a synthetic/virtual clock is used for STA), or add a caveat that an empty string is accepted only in some OpenLane versions.
- [ ] [LOW] L105 — QUOTE: "| OS | Ubuntu 20.04 / 22.04 | — | Docker দিয়ে যেকোনো platform-এ চলে |". — PROBLEM: Inconsistent/contradictory requirement presentation: the row pins the OS to "Ubuntu 20.04 / 22.04" in the "minimum" column yet the rationale column says it runs on "যেকোনো platform" (any platform) via Docker. The other rows give a real minimum vs recommended (8 GB vs 16 GB); here the "recommended" cell is "—" while the minimum names two specific Ubuntu versions, which conflicts with the "any platform via Docker" justification and may mislead macOS/Windows/other-Linux readers into thinking only those two Ubuntu releases work. — FIX: Make the OS row consistent — e.g. list it as "Linux/macOS/Windows (via Docker)" or "যেকোনো OS যেখানে Docker চলে", and move the "Ubuntu 20.04/22.04" specifics to a note as merely the tested/reference distros rather than a hard minimum.

## Chapter 23 — Sky130 PDK (10 issues: 3 HIGH, 5 MED, 2 LOW)

- [ ] [HIGH] L319 / L328 — QUOTE: "| `fd` | foundry (SkyWater) |" (and diagram line "│ └──────────────────── foundry (SkyWater)") — PROBLEM: In the Sky130 naming scheme `sky130_fd_sc_hd`, `fd` stands for "Foundry" as a *provider/category* marker (distinguishing foundry-developed IP from third-party IP such as the OSU cells); it does NOT mean "SkyWater". The foundry/process itself is SkyWater's S8/sky130 — the name SkyWater is not encoded by `fd`. The chapter conflates the abbreviation `fd` with the foundry's name in three places (the ASCII diagram L319, the legend table L328, and implicitly L313). This teaches a wrong expansion of the single most-used identifier in the whole PDK. — FIX: Expand `fd` as "Foundry (foundry-provided IP)" and state separately that the foundry is SkyWater; remove the equation `fd = SkyWater`.
- [ ] [HIGH] L654–L681 — QUOTE: "function : \"(A&B)\";" together with only "pin(A) { direction : input; capacitance : 0.0015; }" — PROBLEM: The Liberty example is for `sky130_fd_sc_hd__and2_1`, a 2-input AND whose output function is "(A&B)", yet the cell declares only `pin(A)` as an input — `pin(B)` is entirely missing. A real `.lib` must declare every signal pin used in a function; an AND2 has TWO inputs (A and B). The surrounding prose (L681) even says "function : \"(A&B)\" সাফ জানিয়ে দিচ্ছে cell-টা সত্যিই একটা AND", drawing attention to the very inconsistency. — FIX: Add a `pin(B)` block (direction : input; capacitance : ...) parallel to pin(A) so the example is self-consistent with the two-input AND function.
- [ ] [HIGH] L130 vs L469 — QUOTE: "transistor-এর gate সবচেয়ে ছোট যতটুকু করা যায়, সেটা প্রায় **130 nanometer**" (L130) vs "| Polysilicon | Min width | 150nm |" (L469) — PROBLEM: The gate is made of polysilicon (the chapter itself says so at L219 and L464). So the smallest drawable gate cannot be 130nm while the polysilicon minimum width is 150nm — the two numbers directly contradict each other. The root mistake is treating the "130nm" *node name* as if it were an actual drawable gate length; in Sky130 the node name is a marketing/technology label and the minimum drawn poly (gate) length is 0.15µm = 150nm. — FIX: Clarify that "130nm" is the technology-node name, not a drawable dimension, and that the actual minimum gate/poly length is 150nm; reconcile L130 with the 150nm poly rule.
- [ ] [MED] L633 — QUOTE: "| **Constraint** | tcq (clock-to-Q) | clock edge-এর পর output আসতে কত সময় |" — PROBLEM: clock-to-Q (tcq / tCKQ) is a *propagation delay* — the time from the clock edge until Q is valid — not a timing *constraint*. Constraints are conditions the surrounding logic must satisfy (setup tsu, hold th); clock-to-Q is something the cell *produces*. Listing it under "Constraint" alongside setup/hold misclassifies a delay as a constraint and muddles the very setup/hold-vs-delay distinction the table is meant to teach. — FIX: Move tcq (clock-to-Q) into the "Delay" group (it belongs with tpd/tr/tf); keep only tsu and th under "Constraint".
- [ ] [MED] L714–L715 — QUOTE: "| **FF** | Fast–Fast | সবচেয়ে ভালো (দ্রুত) অবস্থা |" and "| **SS** | Slow–Slow | সবচেয়ে খারাপ (ধীর) অবস্থা |" — PROBLEM: Calling FF "সবচেয়ে ভালো" (best) and SS "সবচেয়ে খারাপ" (worst) is technically wrong. FF is the *worst* case for hold-time violations and for leakage/power; SS is worst only for setup/maximum-delay. No corner is universally "good" or "bad" — that is exactly why all corners must be signed off (the chapter itself stresses this at L735–737 and L814). Labeling FF as simply "best" teaches a misconception that will cause hold-violation surprises. — FIX: Reword as "FF = fastest transistors (worst case for hold-time / highest leakage)" and "SS = slowest transistors (worst case for setup / maximum delay)"; drop the value-laden "best/worst".
- [ ] [MED] L313–L333 — QUOTE: "sky130_fd_sc_hd__and2_1\n└──┬─┘ └┬┘ └┬┘ └┬┘ └─┬─┘ └┬┘" (with legend table listing seven rows incl. "| `__` | নাম থেকে function আলাদা করার separator |") — PROBLEM: The ASCII underline has only six brace/pointer groups, but the legend table immediately below lists seven parts (sky130, fd, sc, hd, __, and2, _1). The `__` separator — explicitly given its own legend row — has NO caret/pointer under it in the diagram, so the diagram and the legend do not match (6 pointers vs 7 labelled parts), and a reader cannot map the rows to the arrows. — FIX: Either add a seventh pointer under the `__` separator in the diagram, or drop the `__` row from the legend so the diagram (6 pointers) and table (6 parts) agree.
- [ ] [MED] L131–L132 — QUOTE: "একটা চুলের ব্যাস প্রায় ৮০,০০০ nm; তার মানে একটা চুলের প্রস্থে প্রায় ৬০০টা এমন transistor পাশাপাশি বসে যায়!" — PROBLEM: The 80,000 / 130 ≈ 600 arithmetic silently equates the 130nm *gate length* with the footprint of a whole transistor laid "পাশাপাশি" (side by side). A real transistor's side-to-side pitch (diffusion + source/drain contacts + poly + spacing) is on the order of ~1µm, not 0.13µm, so only a few tens of complete transistors actually fit across a hair, not ~600. The sentence conflates gate length with transistor width/pitch and overstates the count by ~10x. — FIX: Either say "প্রায় ৬০০টা gate পাশাপাশি" (gates, i.e. gate-length units) or recompute using a realistic transistor pitch (~1µm → roughly 80 transistors); do not present 130nm as a full transistor's width.
- [ ] [MED] L217–L219 vs L415–L416 — QUOTE: device-layer table lists only "N-diffusion … source ও drain", "P-diffusion … source ও drain", "Polysilicon … gate" — PROBLEM: A MOSFET has four terminals — source, gate, drain, AND body/bulk — and the prompt's domain focus explicitly lists "source/gate/drain/body". The chapter never introduces the body/bulk terminal anywhere in the device discussion, even though the SPICE netlist it presents uses four nodes per device ("Xm1 out in gnd gnd nfet_01v8" — the 4th node `gnd` is the body). A learner cannot understand why the SPICE line has a 4th terminal, or why N-well exists, without the body terminal being named. — FIX: Add the body/bulk as the fourth MOSFET terminal (NMOS body→substrate/VSS, PMOS body→N-well/VDD) and tie it to the 4th node in the SPICE example so the netlist's "gnd gnd"/"vdd vdd" makes sense.
- [ ] [LOW] L385 — QUOTE: "নামের `01v8` মানে এই transistor-গুলো প্রায় **1.8 volt**-এ চলার জন্য বানানো" — PROBLEM: The "প্রায়" (approximately) is imprecise. `01v8` encodes the device's *nominal* operating voltage of exactly 1.8V (that is precisely what the digits 01v8 mean); it is not an approximate "around 1.8V" value. The variation lives in the corners (±10%), not in the nominal the name states. — FIX: Drop "প্রায়": "`01v8` মানে nominal 1.8V supply-এ চলার জন্য বানানো"; mention that the ±10% spread is a corner effect, not built into the name.
- [ ] [LOW] L758 — QUOTE: "| BJT transistors | analog amplifier, voltage reference |" — PROBLEM: "BJT transistors" is redundant — the final "T" in BJT already stands for "Transistor" (Bipolar Junction Transistor), so the phrase reads "Bipolar Junction Transistor transistors". (Minor stylistic/terminology slip.) — FIX: Write just "BJT" or "Bipolar transistors (BJT)".

## Chapter 24 — TinyTapeout (10 issues: 4 HIGH, 3 MED, 3 LOW)

- [ ] [HIGH] L277 — QUOTE: "input wire ena, // Enable (always 1)" — PROBLEM: Factually wrong AND self-contradictory. The official TinyTapeout wrapper comments this signal as "will go high when the design is enabled"; `ena` is high ONLY while the central mux has selected your tile and is `0` otherwise. The chapter's own prose says exactly this elsewhere — L128-130 ("যখন তোমার ডিজাইনকে mux select করে, তখন `ena` হয় `1`; না করলে `0`"), L199 ("mux তোমায় select করলে `1`"), and L316-317 ("counter শুধু তখনই বাড়ে যখন `ena` হয় `1`, অর্থাৎ mux যখন তোমার tile-কে select করেছে"). A learner who believes "always 1" would not understand why gating on `ena` matters. — FIX: Change the comment to match the prose and the official template, e.g. `// Enable — high only while this tile is selected by the mux`.
- [ ] [HIGH] L194-196 — QUOTE: "| Input | 8 | `ui[7:0]` | ... | Output | 8 | `uo[7:0]` | ... | Bidirectional | 8 | `uio[7:0]` |" — PROBLEM: Wrong port names. The actual TinyTapeout signal names are `ui_in[7:0]` (dedicated inputs), `uo_out[7:0]` (dedicated outputs), and the bidirectional bus is `uio_in[7:0]` / `uio_out[7:0]` (+ `uio_oe[7:0]`). There is no signal literally named `ui`, `uo`, or `uio` in the module. The chapter's OWN Verilog template (L272-276) correctly uses `ui_in`, `uo_out`, `uio_in`, `uio_out`, so the "IO চুক্তি" table contradicts the code that is supposed to be "হুবহু সেই IO চুক্তি" (L309). — FIX: Rename table rows to `ui_in[7:0]`, `uo_out[7:0]`, `uio_in[7:0]`/`uio_out[7:0]` to match the actual ports and the template.
- [ ] [HIGH] L201 — QUOTE: "মোট **২৭** সিগন্যাল লাইন, আর এটা **fixed** — তুমি বাড়াতে-কমাতে পারবে না।" — PROBLEM: Wrong count, because the IO table (L194-199) silently omits the `uio_oe[7:0]` direction-enable bus, which the chapter itself introduces in the very next callout (L204-207) and declares in the template (L276). The real fixed interface is ui_in(8) + uo_out(8) + uio_in(8) + uio_out(8) + uio_oe(8) + ena(1) + clk(1) + rst_n(1) = 43 wires, not 27. Calling 27 the complete "fixed" contract "একটাও কম নয়, বেশিও নয়" (L190) is therefore incorrect — it is short by the entire 8-bit `uio_oe` (and treats the bidirectional bus as a single 8-bit group rather than separate in/out/oe paths). — FIX: Either add a `uio_oe | 8` row and state the correct total (43 wires), or change the sentence to count IO *pins* (24 user pins) rather than module signal lines; do not call 27 the fixed signal-line total.
- [ ] [HIGH] L84 / L603 / L618 / L765 / L829 — QUOTE: "⏳ অপেক্ষা: ৬–৮ মাস<br/>(ওয়েফার তৈরি হয়)" (L84) vs "চিপ হাতে পাওয়া পর্যন্ত মোটামুটি **৬-১০ মাস**" (L603) — PROBLEM: The timeline figures are mutually inconsistent. L84 says wafer fabrication ALONE is 6–8 months, but the detailed timeline mermaid shows fabrication as "মাস ২–৭" (L609) = exactly 6 months, and L618 restates fab as months 2–7. Meanwhile the chip-arrival total is given three different ways: "৬-১০ মাস" total (L603, mermaid ending at month 10), "৬-৮ মাস পরে... চিপ এসে পৌঁছায়" (L765), and "৬ মাস অপেক্ষা" (L829). If fabrication alone is 6–8 months, the end-to-end total cannot also start at 6 months once verify + test + packaging + shipping are added. (Official guidance: fab ~6–9 months, total up to ~1 year.) — FIX: Pick one consistent set, e.g. fab ≈ 6 months (months 2–7), total ≈ 9–12 months to hand, and make L84, L603, L765, and L829 all agree.
- [ ] [MED] L536 / L122-126 / L146 — QUOTE: "✅ PCB board | চিপ বসানোর ছোট্ট বোর্ড" (L536) — PROBLEM: The chapter never mentions the RP2040 microcontroller, even though the demo/carrier PCB you receive is built around an RP2040 that drives the mux address-select lines (the official docs: "a microcontroller (an RP2040, like in the Raspberry Pi Pico)" manages the multiplexer). The chapter explains the mux abstractly — "বাইরে থেকে তুমি একটা **address** পাঠাও" (L123) and "🎚️ Address: 'tile #2 চালাও'" (L146) — but never says *what* sends that address. Naming the RP2040 closes the loop between the multiplexing explanation and the physical board the learner gets. — FIX: State that the included PCB carries an RP2040 microcontroller which generates the clock and selects the active tile via the address bus.
- [ ] [MED] L446-451 — QUOTE: "project:\n title: \"My RISC-V Processor\"\n author: \"Your Name\"\n description: \"Simplified RISC-V core\"\n language: \"Verilog\"\n clock_hz: 10000000" — PROBLEM: This `info.yaml` example omits fields that TinyTapeout actually requires for the CI/GDS flow to work — notably `top_module` (must start with `tt_um_` and be unique on the shuttle) and `source_files` (the list of Verilog files to build), plus a `pinout` section. A learner copying only these five keys would have CI fail, contradicting the surrounding claim that this file is what "TinyTapeout-কে বলে দেয় ডিজাইনটা কার, কী করে" (L435-436). — FIX: Add `top_module: tt_um_your_design`, `source_files: [tt_um_your_design.v]`, and note the `pinout` section, or explicitly say the snippet is abridged and link the template's full `info.yaml`.
- [ ] [MED] L362-368 / FAQ — QUOTE: "`CLOCK_PERIOD \"100\"` | ১০০ ns, মানে ১০ MHz clock | ছোট/নিরাপদ গতি — timing মেলানো সহজ" — PROBLEM: Presenting 10 MHz as the de-facto target without context is misleading: the TinyTapeout clock can run up to ~66 MHz and the project's `clock_hz` (set to 10000000 in info.yaml at L451) is the declared operating frequency reported on the datasheet/website, not just an OpenLane convenience. The prose "clock fast করা পরের চ্যাপ্টারের গল্প" (L368) implies you can speed it up later, but the fabricated tile's max usable frequency is fixed at tape-out by the timing you close here. — FIX: Note that 10 MHz is a conservative *example* and that the closed timing here sets the silicon's usable clock (it cannot be raised after fab); mention the ~66 MHz pad limit.
- [ ] [MED→LOW] L255 — QUOTE: "| GPIO | পূর্ণ | **8-bit** | tile-এর পিন তো ৮টাই |" — PROBLEM: The justification is technically wrong: a tile does not have "only 8 pins." It has 8 dedicated inputs + 8 dedicated outputs + 8 bidirectional = 24 user IO pins (plus clk/rst_n/ena). 8-bit GPIO maps to the 8 `uio` pins specifically, not to a tile-wide 8-pin limit. As written it understates the interface and conflicts with the IO contract section. — FIX: Change rationale to e.g. "the 8 bidirectional `uio` pins map naturally to 8-bit GPIO" rather than claiming the tile has only 8 pins.
- [ ] [LOW] L257-261 vs L728-734 — QUOTE: "🎯 এটা একটা **সত্যিকারের কাজ করা RISC-V core** — খেলনা নয়।" (L259) vs "🎯 এটা একটা **আসল RISC-V core** — খেলনা সিমুলেশন নয়।" (L730) — PROBLEM: Two nearly identical motivational bullet lists appear ~470 lines apart (also "C কোড compile করে চালাতে পারে"/"আসল প্রোগ্রাম চালাতে পারে", and "আসল সিলিকনে বসছে"). The repetition is redundant within one chapter and reads as copy-paste padding. — FIX: Trim or differentiate one of the two lists so the same three claims are not restated almost verbatim.
- [ ] [LOW] L276 — QUOTE: "output wire [7:0] uio_oe, // IOs: Enable (1=output)" — PROBLEM: The inline comment drops the input case, so a beginner cannot tell what `0` does from the comment alone. The official template documents both states: "active high: 0=input, 1=output". The explanatory callout (L206-207) does cover both directions, but the in-code comment — the thing readers copy — is incomplete. — FIX: Change to `// IOs: Enable (active high: 0=input, 1=output)` to match the template and remove ambiguity.

## Chapter 25 — Chip Fabrication & Testing (10 issues: 3 HIGH, 5 MED, 2 LOW)

- [ ] [HIGH] L718 — QUOTE: "mips = (cycles / seconds) / 1_000_000" then `print(f"Dhrystone MIPS: {mips:.2f}")` — PROBLEM: This computes (clock cycles ÷ seconds) ÷ 1e6, which is the CLOCK FREQUENCY in MHz, not MIPS. MIPS = (instructions executed ÷ seconds) ÷ 1e6 and requires the retired-instruction count, not the cycle count. The two are equal only when IPC = 1, yet the datasheet (L969) states "IPC: 0.8 average". So the variable is mislabeled "Dhrystone MIPS" while actually holding frequency. The chapter even contradicts this in the warning box L744-748 ("cycle count আর ... clock frequency দিয়ে হিসাব করাই বেশি নির্ভরযোগ্য"), confirming cycles/seconds = frequency. — FIX: Use an instruction counter, e.g. `mips = (tt.get_instruction_count() / seconds) / 1_000_000`, or relabel the printed value as "Clock frequency (MHz)". Reconcile with IPC: MIPS ≈ frequency_MHz × IPC.
- [ ] [HIGH] L73 / L1138 — QUOTE: "Total: Almost 2 years!" (L73, with timeline "Month 22: CHIP ARRIVES! 🎉" L71) vs "Total Learning Time: 12-18 months" (L1138, Final Stats) — PROBLEM: Direct numeric self-contradiction within the same chapter. The opening Timeline runs to Month 22 ("Almost 2 years"), but the closing Final Stats says the whole journey is "12-18 months". 22 months ≠ 12-18 months. This also clashes with CLAUDE.md's "Learning Progression: 12-18 months total". — FIX: Pick one figure consistently. If learning is 12-18 months and fabrication wait adds ~6-8 months, state "12-18 months learning + ~6-8 months fab wait ≈ up to ~2 years total" in BOTH places, or change the Timeline total to match the Final Stats.
- [ ] [HIGH] L727-729 — QUOTE: "sim_perf = 35 # Your simulation results" / "print(f\"Simulation: {sim_perf} MIPS\")" — PROBLEM: 35 is the chip's clock FREQUENCY (datasheet L957: "Frequency: 35 MHz (tested)"), not its MIPS. The datasheet's actual Dhrystone figure is "Dhrystone: 28 MIPS" (L967). So the benchmark hardcodes 35 "MIPS" while the datasheet says the chip does 28 MIPS — the code conflates 35 MHz with 35 MIPS and contradicts the datasheet. — FIX: Set `sim_perf` to the simulated Dhrystone MIPS (e.g. 28, matching the datasheet) and keep frequency (35 MHz) as a separate quantity.
- [ ] [MED] L963 — QUOTE: "Memory: 256 bytes I-mem, 256 bytes D-mem" — PROBLEM: Chapter 22 (the design that is taped out) specifies "512 byte করে" with code comments "512 byte instruction memory / 512 byte data memory" (Chapter_22_OpenLane_Physical_Design.md L434, L449-450). The datasheet here halves both to 256 bytes. Same chip, two different memory sizes across chapters. — FIX: Make the datasheet match the actual taped-out design (512 B I-mem, 512 B D-mem), or correct Chapter 22 — and use one figure book-wide.
- [ ] [MED] L4-6 / L34 / L69-71 — QUOTE: "6 months later... THE CHIP ARRIVES!" (L4) vs "Prerequisites: Chapter 24 complete + 6-8 months patience!" (L34) vs Timeline "Month 14-20: Fabrication at SkyWater fab" + "Month 22: CHIP ARRIVES!" (submitted Month 13, L68) — PROBLEM: Three different fab/wait durations. The banner says 6 months; the prereq says 6-8 months; but the Timeline implies submit at Month 13 → arrives Month 22 = ~9 months. Inconsistent. — FIX: Standardize the fab-wait figure (e.g. 6-8 months) and adjust the Timeline so submit-to-arrival equals it.
- [ ] [MED] L69 / L122 — QUOTE: "Month 14-20: Fabrication at SkyWater fab" (L69) and "একটা ২০০ বা ৩০০ mm ব্যাসের ... সিলিকনের গোল চাকতি" (L122) — PROBLEM: SkyWater (the fab for the Sky130 PDK / TinyTapeout) is a 200 mm (8-inch) facility; Sky130 wafers are 200 mm. Stating the wafer is "200 or 300 mm" is wrong for this specific flow — 300 mm does not apply to SkyWater/Sky130. — FIX: State "200 mm (Sky130 / SkyWater is a 200 mm process)"; optionally note 300 mm is used by leading-edge fabs generally, but not here.
- [ ] [MED] L170-188 vs L193-201 — QUOTE: box "What You Receive" lists "✅ USB connector / ✅ Pin headers / ✅ Datasheet / ✅ Test guide" (no RP2040), but the explanatory table that claims to cover "বাক্সের প্রতিটা জিনিস" (L190) adds a row "RP2040 MCU" (L197) — PROBLEM: The table introduces an item (RP2040 MCU) that is NOT in the box list, while the box's "Already mounted on PCB!" (L175) item has no table row. The "each item of the box" framing is broken — box and table do not match. — FIX: Add "RP2040 MCU" to the ASCII box list (it is on the PCB), and make the table rows correspond one-to-one to the box contents.
- [ ] [MED] L969 vs L714-718 — QUOTE: datasheet "IPC: 0.8 average" (L969) vs benchmark deriving MIPS from "cycles = tt.get_cycle_count()" (L714) as if cycles == instructions — PROBLEM: Computing MIPS from a CYCLE count assumes IPC = 1, but the datasheet declares IPC = 0.8. The performance code and the datasheet make incompatible assumptions about instructions-per-cycle. — FIX: Either count retired instructions for MIPS, or explicitly compute MIPS = frequency × IPC = freq_MHz × 0.8, keeping it consistent with the stated IPC.
- [ ] [LOW] L147-148 — QUOTE: "একটা ওয়েফারে ১০০+ photolithography, etching আর doping ধাপ পরপর চলে" — PROBLEM: "100+" is an overstatement for a 130nm node like Sky130, which typically involves on the order of ~40-60 mask/photolithography steps (and a comparable handful of etch/implant operations per layer). "100+" is closer to leading-edge sub-10nm processes. — FIX: Soften to "কয়েক ডজন (প্রায় ৪০-৬০টি) photolithography ধাপ", or qualify that 100+ applies to advanced modern nodes, not 130nm.
- [ ] [LOW] L730-733 — QUOTE: "print(f\"Ratio: {perf/sim_perf:.2%}\")" with comment "# Usually 80-95% of simulation" — PROBLEM: Because of the bug in #1/#3, `perf` ends up ≈ the clock frequency (~35) and `sim_perf` is hardcoded to 35, so the printed Ratio would be ~100%, directly contradicting the "80-95%" comment right below it. The example therefore demonstrates the opposite of what its own narration (L740-742) claims. — FIX: After fixing the MIPS computation (#1) and `sim_perf` (#3, e.g. 28), the real-vs-sim ratio will land in the stated 80-95% band; verify the numbers in the example produce a ratio consistent with the comment.
